# URL base da API (F7-03 do ROADMAP.md — usada pelo HttpClient em src/adapters/http/httpClient.ts a partir de F8).
# Copie este arquivo para .env e ajuste o valor para o seu ambiente.
VITE_API_BASE_URL=