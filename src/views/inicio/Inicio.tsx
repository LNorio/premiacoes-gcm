/**
 * Painel Geral (view padrão do Shell). O ROADMAP.md não tem uma subtarefa
 * própria para o conteúdo desta tela (cartões com estatísticas reais, como
 * no protótipo) — fica como placeholder mínimo até isso ser definido/
 * agendado explicitamente no roadmap.
 */
export function Inicio() {
  return (
    <section className="view">
      <div className="view-cabecalho">
        <h2>Painel Geral</h2>
        <span className="view-subtitulo">Visão resumida da filial selecionada</span>
      </div>
    </section>
  );
}
