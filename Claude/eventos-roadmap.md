# Eventos do Roadmap

## Marcos alcançados

### 2026-08-10 — F0: Fundação e arquitetura

- Stack definida e registrada (React + TypeScript + Vite) em `Claude/ARQUITETURA.md`.
- Convenção de pastas em camadas criada em `src/` (`components/ui`, `services`, `adapters`, `types`, `utils`, `views`, `styles`).
- Tokens de design (paleta, tipografia, espaçamento, raios, sombras) extraídos do protótipo para `src/styles/tokens.css` e `src/styles/global.css`.
- Catálogo de componentes visuais base implementado em `src/components/ui/`: `Button`, `Card`/`CardGrid`, `BadgeInfo`/`BadgeTela`/`Selo`, `FloatingField`, `Table`/`LinhaVazia`, `Toast`, `Header`, `Nav`.
- Documento de arquitetura em camadas (UI ↔ Serviço ↔ Adapter ↔ HTTP) e a convenção "nenhuma tela chama fetch" escritos em `Claude/ARQUITETURA.md`.
- Vitest + Testing Library configurados; 27 testes unitários cobrindo os componentes base, todos passando (`Claude/testes/f0-fundacao-arquitetura.md`).
- **Pendente dentro do próprio F0:** F0-03 (repositório/CI) e F0-04 (provisionamento de ambientes) não avançaram — dependem de decisões de hospedagem/infraestrutura fora do escopo do código frontend.

### 2026-08-10 — F1: Camada de dados (contrato + mock)

- Tipos/shapes de todas as entidades da Seção 5 do documento técnico definidos em `src/types/`.
- Interfaces de Serviço por domínio (`src/services/`): auth, colaboradores, premiação, consolidado PEV, consulta, comissão, descontos, plano de saúde, estoque e bloqueio.
- Adapter mock completo (`src/adapters/mock/`), persistindo em `localStorage`, com os 6 colaboradores seed do documento técnico (usuários/senhas reais do protótipo).
- Wrapper HTTP genérico preparado e ainda não usado (`src/adapters/http/httpClient.ts`), pronto para F7/F8.
- Formato de retorno padronizado `Resultado<T>` (`src/types/resultado.ts`) e utilitários compartilhados (`src/utils/`): formatação de moeda/mês, máscara de CPF, ciclo PEV, `mostrarToast`, helpers de filial.
- 68 testes unitários no total (27 de F0 + 41 novos), todos passando (`Claude/testes/f1-camada-de-dados.md`).

### 2026-08-10 — F2: Shell, autenticação e cadastros

- Sessão (login/logout/erro/troca de filial) em `src/state/SessaoContext.tsx`, consumindo `authServiceMock`.
- Tela de login (`src/views/auth/Login.tsx`) com campo flutuante e mostrar/ocultar senha.
- Shell do app (`src/views/shell/Shell.tsx`): cabeçalho fixo com ajuste de espaço automático (ResizeObserver), navegação por `NAV_POR_PAPEL`, seletor de filial para o Admin vs. badge fixo para os demais, guarda de rota.
- Componentes de estado carregando/vazio/erro (`src/components/ui/Estado.tsx`) e `ToastHost` para a fila de `mostrarToast`.
- Tela de Cadastro de Colaboradores completa (`src/views/vendedores/CadastroColaboradores.tsx`): formulário com máscara de CPF e checklist de telas, validação, CRUD, visibilidade restrita a Admin numa filial específica.
- 88 testes unitários no total (68 de F0+F1 + 20 novos), todos passando (`Claude/testes/f2-shell-auth-cadastro.md`). Verificação visual real feita rodando o dev server e dirigindo o app com Playwright headless (login → shell → cadastro → logout, sem erros de console).
- **Pendência sinalizada, fora do escopo de F2:** o conteúdo real do Painel Geral (cartões com estatísticas) não tem subtarefa própria no `ROADMAP.md`; `Inicio.tsx` ficou como placeholder mínimo até isso ser agendado.

### 2026-08-10 — F3: Núcleo de Premiação

- Planilha de Premiação (`src/views/premiacao/Premiacao.tsx`): grade editável das 5 categorias, totais/Planilha Deivson em tempo real sem perder foco, bloqueio (Admin bloqueia/desbloqueia, Gerente fica travado quando bloqueado), filtro de mês, exportação CSV.
- Consolidado PEV (`src/views/consolidadoPev/ConsolidadoPev.tsx`): colunas dinâmicas por mês, adiantamento de férias editável só pelo Admin, derivados (Base 28%, Adicional a Receber) recalculados ao vivo, filtros de ciclo/período, exportação CSV.
- Consulta por Período (`src/views/consulta/ConsultaPeriodo.tsx`): cartões por mês, escopo restrito ao próprio vendedor com título "Minhas Premiações por Período", coluna Filial quando Admin vê todas, filtro de período, exportação CSV reaproveitando as mesmas colunas da Planilha.
- Utilitário de exportação CSV (`src/utils/exportar.ts`, `exportarPremiacoesCSV` em `src/services/premiacaoService.ts`) replicando exatamente o padrão da Seção 4 do documento técnico (separador `;`, BOM UTF-8, nome de arquivo).
- As três telas ligadas ao Shell, substituindo o placeholder `EmConstrucao`.
- 107 testes unitários no total (88 de F0+F1+F2 + 19 novos), todos passando (`Claude/testes/f3-nucleo-premiacao.md`). Verificação visual real feita rodando o dev server e dirigindo o app com Playwright headless (Premiação → Consolidado PEV → Consulta, dados fluindo corretamente entre as três telas, sem erros de console).
- **Ao construir estas telas, quatro lacunas/bugs do adapter mock de F1 foram descobertos e corrigidos** (commits `fix(F1)` anteriores a este) — nenhum tinha sido pego pelos testes isolados de F1 porque não exercitavam os cenários reais das telas (Admin em "Todas as filiais", filtro vazio, etc.).

### 2026-08-11 — F4: Comissão e Descontos

- Comissão (`src/views/comissao/Comissao.tsx`): grade Código/Colaborador/CPF/Função/[PEV]/Comissão/Garantido, sem coluna Total; PEV somente leitura (lido da Planilha de Premiação, gravado como snapshot ao salvar) e visível só para o Admin; bloqueio (editor Gerente); filtro de mês; exportação Excel.
- Descontos e Bonificações (`src/views/descontos/Descontos.tsx`): grade agrupada por colaborador com múltiplos lançamentos no mesmo mês, Tipo (lista fixa de 10 opções)/Valor/Observações por lançamento, Total por colaborador na primeira linha do grupo + rodapé geral, adicionar/remover lançamento, bloqueio (editor Coordenador), exportação Excel.
- Utilitário de exportação Excel (`baixarExcel` em `src/utils/exportar.ts`) carregando o SheetJS por CDN sob demanda (mesmo padrão do protótipo, Seção 1), com cache de carregamento que se limpa sozinho se o script falhar, para permitir nova tentativa depois. `exportarComissoesExcel` (`src/services/comissaoService.ts`) e `exportarDescontosExcel` (`src/services/descontosService.ts`) seguem exatamente as colunas da Seção 4 do documento técnico.
- As duas telas ligadas ao Shell, substituindo o placeholder `EmConstrucao`.
- 142 testes unitários no total (107 de F0+F1+F2+F3 + 35 novos), todos passando (`Claude/testes/f4-comissao-descontos.md`). Verificação visual real feita rodando o dev server e dirigindo o app com Playwright headless.
- **Ao construir a tela de Comissão, uma lacuna do adapter mock de F1 foi descoberta e corrigida** (ver seção "Correções em funcionalidades prontas" abaixo) — mesmo padrão dos bugs de `FILIAL_TODAS` já encontrados em F3.

### 2026-08-12 — F2.INICIO: Início (Painel Geral)

- Conteúdo real da tela Início implementado (`src/views/inicio/Inicio.tsx`), que desde F2 era um placeholder mínimo por falta de subtarefa própria no roadmap (registrado no evento de F2 do dia 2026-08-10). Especificação seguida à risca do protótipo (`view-inicio`/`inicio-gestor`/`inicio-vendedor`): painel do gestor (Admin/Gerente/Coordenador) com cartões Filial/Colaboradores cadastrados/Premiações lançadas/Total a pagar e ações rápidas "+ Cadastrar vendedor"/"+ Preencher planilha do mês"; painel do vendedor com Minha filial/Minha função/Premiações recebidas/Total a receber e a ação "Ver minhas premiações por mês". `Shell.tsx` passou a repassar `aoNavegar={setViewAtiva}` para a tela trocar de aba a partir dos atalhos.
- Reaproveita `colaboradoresService`/`premiacaoService` já existentes — sem serviço próprio, como decidido com o usuário antes de implementar (a tarefa não estava no `ROADMAP.md`; adicionada como `F2.INICIO` a pedido do usuário antes de prosseguir).
- **Decisão consultada com o usuário:** o protótipo soma premiações de **todos os meses já lançados** (sem filtro de data) para os cartões de premiação; os serviços atuais (mock e HTTP) só buscam um mês por vez, e não há uma consulta "sem filtro de data" validada contra a API real. A pedido do usuário, os cartões usam o **mês atual** (`obterMesAtualISO`) em vez do histórico completo — muda a semântica do cartão frente ao protótipo, mas evita uma consulta potencialmente lenta/instável contra a API real.
- "Premiações lançadas"/"Premiações recebidas" contam só lançamentos com `total > 0` (não a quantidade de colaboradores habilitados para a tela) — necessário porque `premiacaoServiceHttp` devolve uma linha zerada para todo colaborador habilitado sem lançamento no mês (ver F8.PREM), então contar `.length` direto inflaria o número.
- 12 testes novos (`Claude/testes/f2-inicio-painel-geral.md`; `Inicio.test.tsx` + ajuste em `Shell.test.tsx`, que também mostrava "Filial 100" e passou a escopar a busca ao cabeçalho `role="banner"` para não colidir com o novo cartão da Início).

### 2026-08-12 — Logo da Comercial Mariano no login e no cabeçalho

- **O que foi modificado:** adicionados `src/assets/logo-comercial-mariano.png` (versão completa, com slogan "Sua confiança é o que nos move!") e `src/assets/logo-gcm.png` (versão compacta), fornecidos pelo usuário. `Login.tsx` passou a mostrar o logo completo acima do título "Sistema de Premiações" (`.login-logo`, já definida em `Login.css` desde F2 mas sem imagem nenhuma até agora). `Shell.tsx` passou a preencher o slot `logo` do `Header` com o logo compacto (`.cabecalho-logo`, classe já existente em `Header.css` desde F0, também sem uso até agora).
- **Motivo:** pedido do usuário, replicando os slots `.login-logo`/`.cabecalho-logo` já previstos desde o protótipo mas nunca preenchidos.
- **Impacto:** nenhuma mudança de lógica — só os dois `<img>`. Verificado visualmente (Playwright) que o logo completo renderiza corretamente na tela de login; o logo do cabeçalho não pôde ser confirmado visualmente na mesma sessão porque a API real ficou indisponível no momento do teste — a mudança usa o mesmo padrão (`Header`'s slot `logo`, já testado) e passou em `tsc`/`vitest`/`oxlint`/`build` sem erros.

### 2026-08-12 — F5: Plano de Saúde

- **Cadastro (Titulares e Dependentes)** — `src/views/planoSaude/CadastroTitulares.tsx`: titulares vêm do Cadastro de Colaboradores (habilitados para a tela `planoSaude`); dependentes adicionados/removidos por modal (nome + CPF, CPF opcional); duas checkboxes de adesão por titular (Saúde/Odontológico, `undefined` tratado como aderido); só o Admin edita adesão e gerencia dependentes — Coordenador vê tudo somente leitura (checkboxes `disabled`, sem os botões "+ Dependente"/"Remover"). Linhas de dependente mostram a adesão da família como texto (✓/—), não uma checkbox própria — eles não têm adesão independente do titular.
- **Lançamento (Saúde/Odontológico)** — `src/views/planoSaude/LancamentoPlanoSaude.tsx`: sub-sub-abas Plano de Saúde/Plano Odontológico; uma linha por pessoa (titular, depois cada dependente), só famílias com adesão ao tipo selecionado; valor de Titular/Dependente sempre fixo por filial (R$185,27 padrão, R$255,54 nas filiais 401/403) e por tipo (R$13,56 no Odontológico), nunca digitado; célula `***` (`.celula-bloqueada`, já existente desde F0) na coluna que não corresponde ao tipo da pessoa da linha. Plano Odontológico não tem nenhum campo editável (só o valor fixo) — botão de salvar e de bloqueio ficam ocultos nessa sub-aba, só aparecem em Saúde (onde há `valorAdicional`/`valorCoparticipacao`, editáveis por pessoa). Bloqueio: tela `planoSaude`, editor Coordenador. Exportação Excel respeita a sub-aba ativa (colunas diferentes para Saúde × Odontológico) e a filial.
- Container `src/views/planoSaude/PlanoSaude.tsx` com as duas sub-abas (Titulares e Dependentes / Lançamento); `Shell.tsx` ligado (`case "plano-saude"`), substituindo o placeholder `EmConstrucao`.
- **Decisão de implementação — contradição entre o documento técnico e o protótipo (Seção 3.6.2):** uma frase do documento técnico dizia que a coluna "Descrição" (TITULAR/DEPENDENTE) seria "editável só pelo Administrador", mas outra frase no mesmo parágrafo e o código do protótipo (`renderizarLancamentoPlanoSaude()`) mostram a Descrição sempre como texto fixo, nunca um campo — e a API não tem endpoint nenhum para persistir isso. Implementado seguindo o protótipo (texto fixo, nunca editável) por ser o código executável/fonte de verdade já usada em todo o resto do projeto; `ROADMAP.md` (F5.PS-LAN-04) atualizado para refletir essa decisão.
- **Correção de scaffolding de F1:** `PlanoSaudeLancamento` (`src/types/planoSaude.ts`) tinha os campos `valorTitular`/`valorDependente` como obrigatórios, mas nem o protótipo nem a API real (`Claude/API (3).md`, `PUT /api/lancamentos`) persistem esses valores — são sempre recalculados na leitura a partir da filial/tipo. Campos removidos do tipo; `titularId` adicionado (usado para agrupar dependentes por família).
- 19 testes novos (`CadastroTitulares.test.tsx`, `LancamentoPlanoSaude.test.tsx`, `PlanoSaude.test.tsx` + 1 caso novo em `Shell.test.tsx`) — ver `Claude/testes/f5-plano-saude.md`. Um teste pegou um bug real antes de qualquer commit: o total por linha usava o lançamento já salvo (desatualizado) em vez do valor em edição, então o total não reagia ao que o usuário acabava de digitar — corrigido antes de finalizar a tela.
- Achado à parte, sem relação com Plano de Saúde: rodar a suíte completa revelou dois testes quebrados por um rótulo já renomeado pelo usuário diretamente no código (`Premiacao.tsx`/`Comissao.tsx`: "PEV" → "PEV Atingida", "Comissão" → "Comissão (PEV Base)") — `Premiacao.test.tsx` e `Comissao.test.tsx` atualizados para os rótulos atuais.

### 2026-08-13 — F6 (Premiações Estoque) decidido como não implementado por enquanto

- **O que foi modificado:** `NAV_POR_PAPEL.admin` (`src/utils/constantes.ts`) deixou de incluir `"premiacao-estoque"` — a aba "Premiações Estoque" some da barra de navegação do Admin (era o único papel que a via). A guarda de rota do Shell já redireciona para `inicio` qualquer tentativa de acessar uma view fora de `NAV_POR_PAPEL`, então a tela também deixa de ser alcançável mesmo por navegação direta. O tipo `Tela` e `ROTULOS_TELAS["premiacao-estoque"]` não foram removidos (ainda são referências válidas, só não aparecem mais na navegação).
- **Motivo:** pedido do usuário — F6 não será implementado num primeiro momento (já estava marcado como "condicional à decisão de negócio" no `ROADMAP.md`; esta é a decisão).
- **Impacto:** `ROADMAP.md` (cabeçalho de F6) atualizado para refletir a decisão. Teste de `Shell.test.tsx` ("Admin vê todas as 9 abas") ajustado para 8 abas, com uma checagem explícita de que "Premiações Estoque" não aparece mais.

### 2026-08-13 — Total de Descontos e Bonificações passa a somar Bonificação/Ajuda de Custo e subtrair os demais tipos

- **O que foi modificado:** `somarDescontosComSinal` (novo, `src/services/descontosService.ts`) usado no Total por colaborador e no rodapé "Total geral" de `Descontos.tsx` (F4.DESC-05) — lançamentos do tipo "Bonificação" ou "Ajuda de Custo/Gratificação" somam o total normalmente; os demais 8 tipos (Compra de mercadorias, Convênio Gás, Desconto autorizado, Diária, Farmácia, Franquia, Manutenção veículos, Multa) passam a subtrair. A célula "Valor" de cada lançamento e o que é enviado a `salvarDescontos`/à API continuam exatamente como digitado (sempre positivo) — só a exibição do Total muda.
- **Motivo:** pedido do usuário.
- **Impacto:** o Total por colaborador e o Total geral podem aparecer negativos quando os descontos superam bonificações/ajudas de custo no mês (`formatarMoeda` já formata negativos corretamente, ex. "-R$ 50,00"). Três testes novos em `Descontos.test.tsx` cobrindo o novo cálculo; dois testes existentes que somavam valores sem selecionar um Tipo (ficando com o sinal padrão de "desconto") foram ajustados para selecionar "Bonificação" explicitamente, mantendo a intenção original de testar soma simples.
- **Correção no mesmo dia:** valores negativos mais largos (ex. "-R$ 2.400,00") quebravam em duas linhas dentro da célula de Total, com o sinal "-" isolado numa linha e o valor na outra. `.celula-numerica` (`src/components/ui/Table.css`) ganhou `white-space: nowrap` — afeta todas as colunas numéricas do app (a tabela já tem `overflow-x: auto` no wrapper, então uma coluna genuinamente larga demais gera rolagem horizontal em vez de quebrar o texto).

## Alterações não planejadas

### 2026-08-10 — Campo de Filial no cadastro de colaboradores

- **O que foi modificado:** o formulário de Cadastro de Colaboradores (`src/views/vendedores/CadastroColaboradores.tsx`, `F2.CAD-04`) ganhou um campo `<select>` de Filial, preenchido por padrão com a filial ativa da sessão (ou a primeira filial, quando o Admin está em "Todas as filiais"). O formulário deixou de ficar bloqueado com aviso quando o Admin está em "Todas as filiais" — agora ele fica disponível e cada colaborador é salvo com a filial escolhida no próprio formulário, não mais implícita pelo cabeçalho.
- **Motivo:** pedido do usuário — ao cadastrar um usuário, é preciso indicar explicitamente a qual filial ele está vinculado, inclusive quando o Admin está vendo todas as filiais ao mesmo tempo.
- **Impacto:** `salvarColaborador` passou a receber `filial: formulario.filial` em vez de `sessao.filialAtiva`; a listagem ganhou uma coluna Filial quando o Admin está em "Todas as filiais".

### 2026-08-10 — Modal para adicionar/editar colaborador

- **O que foi modificado:** o formulário inline de Cadastro de Colaboradores (linha de `<input>`s no topo da tabela) foi substituído por uma modal. Um botão "+ Adicionar colaborador" abre a modal em branco; o botão "Editar" de cada linha abre a mesma modal pré-preenchida; salvar ou fechar (X, Escape, clique fora) fecha a modal. Novo componente base `src/components/ui/Modal.tsx` (`createPortal`, `role="dialog"`, fecha em Escape/overlay/X), com testes próprios.
- **Motivo:** pedido do usuário — o formulário inline ocupava espaço permanente no topo da tela mesmo quando ninguém estava cadastrando; a modal deixa a tabela como foco principal da tela.
- **Impacto:** `CadastroColaboradores.tsx` ganhou `abrirParaAdicionar`/`abrirParaEditar`/`fecharModal` e estado `modalAberto`; os campos do formulário passaram de células de tabela para divs `.grade-formulario`/`.campo` dentro da modal; `src/components/ui/index.ts` passou a exportar `Modal`.

### 2026-08-10 — Coluna Filial na Planilha de Premiação e no Consolidado PEV

- **O que foi modificado:** a Planilha de Premiação (`src/views/premiacao/Premiacao.tsx`, F3.PREM-03) e o Consolidado PEV (`src/views/consolidadoPev/ConsolidadoPev.tsx`, F3.PEV-03) ganharam uma coluna Filial, visível apenas quando o Admin está em "Todas as filiais" (mesmo padrão já usado no Cadastro de Colaboradores). `LinhaConsolidadoPev` (`src/services/consolidadoPevService.ts`) ganhou o campo `filial`, populado em `src/adapters/mock/consolidadoPevService.mock.ts` a partir do colaborador de cada linha.
- **Motivo:** pedido do usuário — com o Admin vendo todas as filiais ao mesmo tempo nessas duas telas, era preciso identificar visualmente a qual filial cada linha pertence (a Planilha de Premiação já resolvia isso agrupando por `vendedor.filial` internamente, mas não expunha a coluna).
- **Impacto:** nenhuma mudança de contrato para os demais perfis (a coluna só aparece para o Admin em "Todas as filiais"); testes de `Premiacao.test.tsx` e `ConsolidadoPev.test.tsx` atualizados para cobrir a nova coluna.

### 2026-08-11 — Colaboradores consumindo a API real (F8.CAD, adiantado)

- **O que foi modificado:** `colaboradoresServiceHttp` (`src/adapters/http/colaboradoresService.http.ts`) implementado contra `GET/POST/PUT/DELETE /api/usuarios` (`Claude/API.md`) e ligado como `colaboradoresService` fora dos testes (`src/adapters/index.ts`); `CadastroColaboradores.tsx` ajustada para não exigir senha ao **editar** (só ao criar) — a API nunca devolve a senha existente (campo write-only), então o formulário agora mostra o campo em branco na edição e só reenvia `senha` se o usuário digitar uma nova.
- **Motivo:** pedido do usuário, seguindo a mesma trilha adiantada de F7/F8 já usada para o login (F7-01).
- **Riscos e limitações conhecidas, investigados ao vivo contra a API antes de implementar:**
  1. **IDs numéricos de `telas` não documentados.** `POST /api/usuarios` só aceita `telas` como array de IDs numéricos (`colaborador_has_telas.telas_id` é `bigint` — confirmado por um erro real de banco ao tentar enviar strings). Não há endpoint para consultar o mapeamento id → tela. **A pedido do usuário, o código assume a mesma ordem já usada internamente** (`premiacoes=1, comissao=2, planoSaude=3, estoque=4, descontos=5`, ver `ID_TELA` em `colaboradoresService.http.ts`) — **isso pode estar errado** e gravar um colaborador na tela errada silenciosamente até ser confirmado com quem mantém o backend.
  2. **A criação (`POST`) não retorna o registro criado.** Sem `id`, o adapter faz um segundo `GET /api/usuarios?filial=...` e casa pelo campo `usuario` (único) para descobrir o id real — funciona, mas custa uma requisição extra a cada cadastro.
  3. **`POST`/`PUT` sem transação no backend.** Ao testar um `telas` inválido, o colaborador foi gravado mesmo assim e só a associação de telas falhou, deixando um registro incompleto (limpo manualmente via `DELETE` durante a investigação). Se o mapeamento de IDs do item 1 estiver errado, o mesmo pode acontecer em uso real.
  4. **`GET /api/usuarios` devolve todos os papéis (admin/gerente/coordenador/vendedor) juntos**, sem paginação. Inicialmente o adapter filtrava `role === "vendedor"` no cliente; em 2026-08-11 esse filtro foi removido a pedido do usuário (ver campo Perfil abaixo) e a tela passou a gerenciar os 4 papéis.
  5. **Dados de teste órfãos na API real:** durante a investigação de `GET /api/premiacoes` (ver item de Premiação abaixo), foram criados lançamentos reais de agosto/2026 para os colaboradores 4 (Carlos) e 5 (Fernanda) que não puderam ser removidos — a API não tem `DELETE` de premiações.
- **Premiação (F8.PREM) não foi implementada nesta rodada:** `GET /api/premiacoes` não devolve nenhum id de colaborador na resposta (só `"nome colaborador"`) e agrega os valores pelo período pedido, em vez de expor um registro por colaborador/mês pronto para editar. A pedido do usuário, a Planilha de Premiação continua no mock até isso ser resolvido com quem mantém a API (idealmente expondo `id colaborador`/`mes de referencia` por linha). **Atualização em 2026-08-11:** implementada mesmo assim (ver evento abaixo) fazendo uma requisição por colaborador em vez de esperar a correção da API.

### 2026-08-11 — Campo de Perfil no Cadastro de Colaboradores

- **O que foi modificado:** o formulário de Cadastro de Colaboradores ganhou um campo "Perfil" (`<select>` com as opções Vendedor, Coordenador, Gerente, Administrador, nessa ordem — `PAPEIS_COLABORADOR`/`ROTULOS_PAPEL` em `src/utils/constantes.ts`), com "Vendedor" como padrão para novos cadastros. `Colaborador` (`src/types/colaborador.ts`) ganhou o campo `role: Papel`, reaproveitando o tipo já usado em `Sessao`. A listagem ganhou a coluna "Perfil". Como consequência direta, o filtro que escondia colaboradores com papel diferente de vendedor em `colaboradoresServiceHttp.listarColaboradores` (registrado no evento anterior) foi removido — a tela agora exibe e gerencia os 4 perfis.
- **Motivo:** pedido do usuário.
- **Impacto:** os 6 colaboradores seed do mock (`src/adapters/mock/seed.ts`) ganharam `role: "vendedor"`; `colaboradoresServiceHttp.salvarColaborador` passou a enviar `role: colaborador.role` para a API em vez do valor fixo `"vendedor"` usado antes. `authServiceMock` (login sob os testes) não foi alterado — continua resolvendo Admin/Gerente/Coordenador só via `CREDENCIAIS_GESTAO`, então um colaborador criado com Perfil "Administrador" pelo mock não ganha automaticamente as permissões de admin no login mockado (login via API real, fora dos testes, não tem essa limitação).

### 2026-08-11 — Planilha de Premiação e Consolidado PEV consumindo a API real (F8.PREM/F8.PEV, adiantado)

- **O que foi modificado:** `premiacaoServiceHttp` (`src/adapters/http/premiacaoService.http.ts`) e `consolidadoPevServiceHttp` (`src/adapters/http/consolidadoPevService.http.ts`) implementados e ligados fora dos testes, consumindo `GET/PUT /api/premiacoes` e `GET /api/consolidado` / `PUT /api/consolidado/adiantamento` (`Claude/API.md`).
- **Motivo:** pedido do usuário, motivado pela atualização do `Claude/API.md` (novo `POST /api/logout`, filtro `filial` em mais endpoints, papéis de acesso com autorização real). Confirmado ao vivo que nada mudou na resposta de `GET /api/premiacoes` que resolvesse a lacuna registrada no evento anterior — a decisão de implementar mesmo assim foi do usuário.
- **Consolidado PEV — solução limpa, a API tem `id` por linha:** diferente de Premiações, `GET /api/consolidado` devolve `"id"` (id do colaborador) em cada linha, então dá para casar com segurança sem depender de nome. A API não devolve a filial nem `telas.premiacoes` de cada colaborador nessa resposta — o adapter busca `/api/usuarios` em paralelo e cruza pelo id para preencher isso e para decidir quem aparece na tela (só quem tem `telas.premiacoes = true`). Os nomes de mês vêm em português fixos no ciclo Dez→Nov (`"valor dezembro"` … `"valor novembro"`); o adapter mapeia cada um para a chave `"YYYY-MM"` correspondente do ciclo pedido — meses fora do ciclo do `ano` consultado voltam como 0 (a API só cobre um ciclo de 12 meses por vez, então um filtro De/Até que cruze dois ciclos não é totalmente coberto — caso raro, não tratado).
- **Premiação — sem solução limpa, replicado o workaround já desenhado:** como `GET /api/premiacoes` continua sem id nenhum na resposta (só `"nome colaborador"`), `premiacaoServiceHttp.listarPremiacoes` faz **uma requisição por colaborador** (`?id={id}&data_inicio=<mês>-01&data_fim=<último dia do mês>`) em vez de uma chamada só — já que o id pedido em cada requisição é conhecido de antemão, nunca depende de casar por nome. Também não existe id de registro de premiação nessa API; `Premiacao.id` é preenchido com uma chave sintética `"{vendedorId}-{mesReferencia}"`, só para uso interno (React key), sem round-trip ao servidor.
- **Impacto:** filial → filial única (não mais array) já era a suposição usada desde o F8.CAD, sem mudança necessária aqui. Nenhuma tela precisou mudar (interface de serviço idêntica à do mock).
- **Consulta por Período (F8.CONS) não foi implementada nesta rodada:** diferente da Planilha (um valor por colaborador já basta), a Consulta precisa de **um cartão por mês** — e `GET /api/premiacoes` só devolve o total somado do intervalo inteiro pedido, não quebrado por mês. A única forma de montar os cartões seria uma requisição por mês por colaborador, e o filtro padrão da tela é "ver todos os meses" (sem teto), o que poderia significar dezenas/centenas de chamadas por carregamento. Perguntado ao usuário como proceder (limitar o padrão a um intervalo, aceitar o volume de chamadas, ou pausar) — a decisão foi **pausar**; Consulta continua no mock até isso ser retomado.

### 2026-08-11 — Efeito de carregamento em botões e barra global durante chamadas à API

- **O que foi modificado:** `Button` (`src/components/ui/Button.tsx`) ganhou a prop `carregando`, que desabilita o botão, marca `aria-busy` e mostra um spinner (`.spinner-botao`, respeitando `prefers-reduced-motion` via a regra global já existente). Ligado nos botões de ação que disparam chamadas à API em cada tela: Login ("Entrar"), Cadastro de Colaboradores ("Cadastrar"/"Salvar alterações" e "Remover" por linha), Planilha de Premiação ("Salvar planilha do mês" e bloquear/desbloquear), Comissão (idem), Descontos ("Salvar lançamento do mês", "Remover" por linha e bloquear/desbloquear) e Consolidado PEV ("Salvar adiantamentos de férias"). Além disso, `HttpClient.requisitar` (`src/adapters/http/httpClient.ts`) agora incrementa/decrementa um contador global (`src/utils/cargaHttp.ts`, mesmo padrão `useSyncExternalStore` já usado em `utils/toast.ts`) em volta de cada chamada; uma barra fina fixa no topo da tela (`src/components/BarraCarregamentoGlobal.tsx`, montada em `App.tsx`) aparece enquanto essa contagem for maior que zero — cobre login e Shell, já que fica fora do `if (sessao)`.
- **Motivo:** pedido do usuário — com a API real em uso desde F7/F8, a latência de rede (chegou a ~18s numa resposta de erro observada durante os testes de Colaboradores) tornava chamadas sem nenhum indicador visual de progresso.
- **Impacto:** só afeta os adapters HTTP (o mock não passa pelo `HttpClient`, então não aciona a barra global — coerente, já que o mock não tem latência real para comunicar). Nenhuma mudança de contrato nas interfaces de serviço.

### 2026-08-11 — Consulta por Período consumindo a API real (F8.CONS, adiantado — blocker anterior resolvido)

- **O que foi modificado:** `consultaServiceHttp` (novo arquivo `src/adapters/http/consultaService.http.ts`) implementado e ligado fora dos testes (`src/adapters/index.ts`), consumindo `GET /api/premiacoes` (mesmo endpoint de F8.PREM). Extraído um módulo compartilhado `src/adapters/http/respostaPremiacoesAgrupadas.ts` (tipos da resposta + `buscarPremiacoesAgrupadas(colaboradorId, filial, dataInicio?, dataFim?)`), usado tanto por `premiacaoServiceHttp` quanto por `consultaServiceHttp`, já que ambos fazem a mesma requisição por colaborador (`?id=`) e parseiam a mesma resposta.
- **Motivo:** pedido do usuário (`Claude/API (3).md`) — "verifique a nova versão da API e corriga as chamadas aos endpoints necessários". A atualização da API mudou `GET /api/premiacoes` para agrupar a resposta por mês (`"meses": [...]` em vez de `"dados": [...]` plano), confirmado ao vivo contra o backend real. Isso **resolve diretamente o motivo pelo qual F8.CONS tinha sido pausada** (ver evento anterior "Planilha de Premiação e Consolidado PEV..."): antes, uma única chamada por colaborador só devolvia o total somado do período inteiro, sem quebra por mês, o que exigiria uma requisição por mês por colaborador para montar os cartões da Consulta. Com a resposta já vindo quebrada em `meses`, uma única requisição por colaborador (mesmo padrão de F8.PREM) basta — o volume de chamadas volta a ser O(N colaboradores), não O(N×M meses).
- **Impacto:** nenhuma mudança de contrato (`ConsultaService.listarConsulta` idêntica à do mock). `listarConsulta` busca `colaboradoresServiceHttp.listarColaboradores(filial)`, restringe a um único `vendedorId` quando `escopo` é informado (perfil vendedor), faz uma requisição por colaborador via `buscarPremiacoesAgrupadas` e agrupa os resultados num `Map` por `mesReferencia`, ordenado cronologicamente. Como a API ainda não devolve id de colaborador por linha (só nome + filial), o casamento continua sempre por `?id=` conhecido, nunca por nome — mesma cautela já registrada em F8.PREM. `ultimoDiaDoMes` foi extraído de `premiacaoService.http.ts` para `src/utils/periodo.ts` (compartilhado entre os dois adapters).

### 2026-08-12 — Filtros padrão de Consulta por Período e Consolidado PEV

- **O que foi modificado:** `ConsultaPeriodo.tsx` (F3.CONS-08) passou a carregar, no primeiro carregamento, apenas o mês passado (`obterMesPassadoISO`, novo utilitário em `src/utils/periodo.ts`) em vez de todos os meses lançados; o botão "Ver todos os meses" continua limpando o filtro normalmente. `ConsolidadoPev.tsx` (F3.PEV-08) passou a ter os campos "De"/"Até" somente-leitura (`disabled`) — eles deixam de ser editáveis diretamente e agora só refletem o intervalo Dez→Nov do campo "Ciclo" (que continua editável), sincronizados via `useEffect`.
- **Motivo:** pedido do usuário.
- **Impacto:** com "De"/"Até" do Consolidado PEV sempre derivados do Ciclo, o estado `de > ate` (mostrava "O mês 'De' não pode ser posterior ao mês 'Até'.") deixou de ser alcançável pela UI — o código de proteção foi mantido (inofensivo), mas o teste que o exercitava foi trocado por dois testes cobrindo o novo comportamento (valor padrão = ciclo do ano atual, campos desabilitados, e sincronização ao trocar o Ciclo). Testes de `ConsultaPeriodo.test.tsx` que dependiam do filtro vazio por padrão foram ajustados para limpar o filtro explicitamente antes de exercitar cenários com múltiplos meses.

### 2026-08-12 — Comissão e Descontos consumindo a API real (F8.COM/F8.DESC, adiantado)

- **O que foi modificado:** `comissaoServiceHttp` (novo `src/adapters/http/comissaoService.http.ts`) e `descontosServiceHttp` (novo `src/adapters/http/descontosService.http.ts`) implementados e ligados fora dos testes (`src/adapters/index.ts`), consumindo `GET/PUT /api/comissoes` e `GET/PUT/DELETE /api/descontos-bonificacoes` (`Claude/API.md`). `HttpClient.delete` (`src/adapters/http/httpClient.ts`) ganhou um segundo parâmetro opcional de corpo JSON, já que `DELETE /api/descontos-bonificacoes` exige `id_do_desconto`/`id_do_colaborador` no corpo (não na URL) — os demais `DELETE` já existentes (`/api/usuarios/{id}`) continuam sem corpo, sem mudança de comportamento.
- **Motivo:** pedido do usuário, seguindo a mesma trilha adiantada de F7/F8 já usada nas demais telas.
- **Comissão — sem lacuna, `pev` já vem calculado ao vivo pela própria API:** `GET /api/comissoes` devolve `pev` já buscado em Premiações no momento da consulta (documentado em `Claude/API.md`: "o campo pev da resposta é buscado ao vivo em Premiações"), então o adapter só precisa mapear o valor recebido — nenhuma lógica de "snapshot" local é necessária (diferente do comentário original em `src/types/comissao.ts`, que descrevia um snapshot tirado ao salvar; na prática, com a API real, o valor de PEV mostrado é sempre o vigente no momento da leitura, não o congelado no momento do salvamento — a tela não muda de comportamento visível porque já lia PEV "ao vivo" da Premiação separadamente, nunca do campo `Comissao.pev`). A API não devolve a filial de cada colaborador nessa resposta — cruzada com `/api/usuarios`, mesmo padrão do Consolidado PEV. Não existe id de registro de comissão na API — chave sintética `"{vendedorId}-{mesReferencia}"`, mesmo padrão de Premiação.
- **Descontos — API só cria lançamentos, nunca atualiza por id (achado ao vivo, importante):** testado diretamente contra o backend real: enviar `PUT /api/descontos-bonificacoes` para um colaborador que já tinha lançamentos **não substitui nem atualiza os existentes — sempre soma um lançamento novo**, mesmo repetindo os mesmos dados. Não há nenhum id de lançamento no corpo do `PUT` para indicar "isto é uma edição". A única forma de editar um lançamento já salvo é apagá-lo (`DELETE`, que exige `id_do_desconto` + `id_do_colaborador`) e recriá-lo do zero. `descontosServiceHttp.salvarDescontos` foi implementado para sempre fazer isso: todo lançamento que já tem `id` (composto como `"{idColaborador}:{idDesconto}"`, decodificado do array `"descontos e bonificacoes"` de cada colaborador em `GET /api/descontos-bonificacoes`) é apagado e recriado a cada "Salvar", **mesmo que o valor não tenha mudado** — o id interno muda a cada salvamento, mas isso nunca é exposto na tela (não há coluna de id visível). Confirmado ao vivo: `PUT`/`DELETE`/`GET` testados em sequência contra o backend real antes de implementar, dados de teste limpos ao final.
- **Impacto:** nenhuma mudança de contrato nas interfaces de serviço (`ComissaoService`/`DescontosService` idênticas às do mock). `Comissao.filial`/`DescontoBonificacao` sem campo de filial (não precisa, `removerDesconto`/`salvarDescontos` operam por id de colaborador/lançamento, não por filial).

### 2026-08-12 — Resposta de rede desatualizada podia sobrescrever dados mais novos (Premiação, Comissão, Descontos, Consolidado PEV, Consulta, Cadastro de Colaboradores)

- **Erro/incompatibilidade encontrada:** o `useEffect`/função `carregar()` de cada uma dessas seis telas dispara uma nova busca sempre que filtro/filial mudam, mas nunca descartava o resultado de uma busca anterior que ainda estivesse "em voo". Detectado ao verificar ao vivo o Comissão recém-implementado: preencher e salvar os campos Comissão/Garantido logo após abrir a tela (antes de qualquer segunda busca terminar) fazia os campos voltarem a ficar vazios — o valor digitado tinha sido enviado corretamente ao servidor (confirmado via `GET /api/comissoes` direto), mas uma segunda leitura, iniciada ao montar a tela (efeito duplicado pelo StrictMode do React em desenvolvimento) e ainda em andamento, terminava depois e sobrescrevia o estado local com os dados antigos. O mesmo padrão existia, sem guarda nenhuma, nas seis telas que buscam dados via serviço: Planilha de Premiação, Comissão, Descontos, Consolidado PEV, Consulta por Período e Cadastro de Colaboradores — um problema real mesmo fora do StrictMode sempre que o usuário troca o filtro de mês/filial duas vezes rápido e a resposta mais antiga demora mais que a mais nova.
- **O que foi alterado para corrigir:** novo hook `useEfeitoAssincrono` (`src/utils/useEfeitoAssincrono.ts`, com testes) — como `useEffect`, mas a função recebe um `foiCancelado()` que vira `true` assim que o efeito é desmontado ou re-executado. Aplicado nas seis telas: cada `carregar()`/callback do efeito agora checa `foiCancelado()` logo depois de cada `await`/`.then()` e só aplica o `setState` seguinte se ainda for a execução mais recente. Em `Comissao.tsx`/`Descontos.tsx`/`Premiacao.tsx`/`CadastroColaboradores.tsx`, `carregar()` ganhou um parâmetro opcional `foiCancelado` (default a uma função que sempre devolve `false`) para que as chamadas diretas fora do efeito — depois de salvar/remover — continuem aplicando o resultado normalmente, sem serem tratadas como canceláveis.
- **Impacto:** nenhuma mudança de contrato ou de comportamento visível em uso normal — só passa a descartar corretamente respostas de rede desatualizadas. Testes das seis telas continuam passando sem alteração (o mock resolve tudo na mesma "tick", então nunca exercitava essa corrida).

### 2026-08-12 — Atalho "+ Cadastrar vendedor" aparecia para Gerente e Coordenador (`Inicio.tsx`, F2.INICIO)

- **Erro/incompatibilidade encontrada:** o painel do gestor mostrava o atalho "+ Cadastrar vendedor" para os três perfis de gestão (Admin/Gerente/Coordenador), mas `CadastroColaboradores.tsx` só permite adicionar/editar/remover colaboradores para o Admin (`ehAdmin` guarda o formulário e as ações da tabela) — Gerente e Coordenador só têm acesso de leitura àquela tela. Encontrado a pedido do usuário, logo após a implementação de F2.INICIO.
- **O que foi alterado para corrigir:** `Inicio.tsx` — o atalho "+ Cadastrar vendedor" passou a exigir `sessao?.role === "admin"`, igual à restrição já existente em `CadastroColaboradores.tsx`. Dois testes novos em `Inicio.test.tsx` cobrindo Gerente e Coordenador.

### 2026-08-12 — Rótulo da aba "Descontos" na navegação não batia com o protótipo

- **Erro/incompatibilidade encontrada:** o protótipo usa `'Descontos/Bonificações'` como rótulo da aba de navegação (`NAV_ITENS['descontos'].rotulo`), mas `ROTULOS_TELAS.descontos` estava como apenas `"Descontos"`. Apontado pelo usuário.
- **O que foi alterado para corrigir:** `src/utils/constantes.ts` — `ROTULOS_TELAS.descontos` passou a `"Descontos/Bonificações"`. `ROTULOS_TELAS_COLABORADOR.descontos` (rótulo do checklist de telas no Cadastro de Colaboradores, um rótulo diferente) não foi tocado — o pedido foi só sobre a aba da barra lateral. `Shell.test.tsx` atualizado para o novo rótulo.

### 2026-08-14 — Nova sub-aba "Período do Plano" em Plano de Saúde (F5.PS-PER), substituindo os valores fixos de Titular/Dependente, depois migrada para a API real (F8.PS)

- **O que foi modificado:** pedido do usuário não estava no `ROADMAP.md` e contradizia uma decisão anterior já registrada aqui (2026-08-12: "valores fixos por filial/tipo... nunca digitados"). Alinhado com o usuário antes de implementar (ver as duas perguntas de escopo) — optou por formalizar no `ROADMAP.md` como `F5.PS-PER`, com o valor do período cadastrado **substituindo** o valor fixo antigo. Essa 1ª versão (mock-only, com datas inicial/final livres e valor separado de Titular/Dependente) foi **reescrita no mesmo dia**, ainda sem commit, depois que o usuário atualizou `Claude/API (4).md` com o módulo real `Valores de Plano de Saúde` e pediu pra consumir a API de Plano de Saúde inteira — o modelo real é diferente do que tinha sido inventado na 1ª versão:
  - **Fechamento de período, não intervalo livre:** só existe **um período vigente por filial + tipo** por vez; a API não aceita data nenhuma no cadastro (o início é sempre "agora", campo `data_criacao`, gerado pelo servidor) — pra trocar o valor, primeiro **encerra** o vigente (`PUT /api/valores-plano-saude/{id}/encerrar`, grava `data_validade`), só depois cadastra um novo (`POST`). Cadastrar um novo enquanto já existe um vigente é recusado (`400`).
  - **Um valor só, não Titular/Dependente separados:** a API tem um único campo `"valor"` por período, usado igual pros dois — a 1ª versão desta mesma sessão (valores diferentes de Titular/Dependente, nunca commitada) foi abandonada por não ter como ser salva na API real.
  - Tipo `PlanoSaudePeriodo` (`src/types/planoSaude.ts`) reescrito: `valor` (único), `ativo`, `dataCriacao`, `dataValidade` (null enquanto vigente) — no lugar de `dataInicio`/`dataFim`/`valorTitular`/`valorDependente`.
  - `PlanoSaudeService.salvarPeriodoPlanoSaude` passou a receber `(filial, tipoPlano, valor)` direto (sem objeto parcial) e `removerPeriodoPlanoSaude` foi trocado por `encerrarPeriodoPlanoSaude(periodo)` — não existe mais ação de apagar período na tela, só encerrar (preserva histórico).
  - `encontrarPeriodoPlano` (`src/services/planoSaudeService.ts`) buscando por `dataCriacao <= mês` e (`dataValidade` nula ou `>= mês`) — como só pode haver um vigente por vez e o histórico é fechado sequencialmente, no máximo um período bate por mês (sem precisar mais de uma regra de desempate).
  - `src/adapters/mock/seed.ts`: período semeado por filial/tipo agora com `ativo: true`, `dataCriacao: "2000-01-01 00:00:00"`, `dataValidade: null`, `valor` único (mesmos valores de antes) — mantém o comportamento de antes por padrão até o Admin mexer.
  - `CadastroPeriodoPlano.tsx` reescrita: formulário só com "Valor"; tabela com Data de Início / Valor / Situação (Vigente/Encerrado) / Data de Encerramento / ação "Encerrar vigência" (só na linha vigente) — sem botão de remover.
  - **Migração pra API real (F8.PS, adiantado):** `src/adapters/http/planoSaudeService.http.ts` (novo) implementa toda a `PlanoSaudeService` — dependentes (`GET/POST/PUT/DELETE /api/dependentes`), adesão (`PUT /api/usuarios/{id}` com `"plano saude"`/`"plano odontologico"`), lançamentos (`GET/PUT /api/lancamentos`) e período (`GET/POST /api/valores-plano-saude`, `PUT .../encerrar`). Ligado fora dos testes em `src/adapters/index.ts` — Plano de Saúde deixa de ser mock-only (só Estoque e Bloqueio continuam). Os shapes de resposta de `GET /api/dependentes` e `GET /api/lancamentos` não têm exemplo em `Claude/API (4).md` — **confirmados ao vivo contra o backend real** antes de implementar (`GET /api/lancamentos` devolve `{"dados": [...], "total titular", ...}`, cada linha com `"id colaborador"`/`"id dependente"`/`"valor adicional"`/`"valor coparticipacao"`, entre outros campos calculados não usados aqui porque a tela recalcula a partir do período; `GET /api/dependentes` devolve array puro). `GET /api/valores-plano-saude` bate exatamente com o exemplo documentado. Escrita (`POST`/`PUT .../encerrar`/`PUT /api/lancamentos`) **não foi testada ao vivo** nesta rodada (só leitura, pra não alterar dados reais de filial sem necessidade) — shape assumido a partir do contrato documentado.
- **Motivo:** pedido do usuário — primeiro pra tornar o valor do plano/odontológico configurável por filial e por vigência, depois pra consumir a API real de Plano de Saúde recém-atualizada (`Claude/API (4).md`).
- **Impacto:** nenhuma mudança de contrato para quem só lê a tela de Lançamento sem cadastrar período (valores idênticos aos anteriores, graças ao período semeado). Testes atualizados: `CadastroPeriodoPlano.test.tsx` reescrito (7 casos, cobrindo cadastro/recusa de duplicidade/encerrar), 2 testes de `PlanoSaude.test.tsx` (sub-aba só pro Admin) inalterados, `LancamentoPlanoSaude.test.tsx` inalterado e passando (o período semeado mantém os mesmos valores) — ver `Claude/testes/f5-plano-saude.md`. Novo `planoSaudeService.http.test.ts` (10 testes) cobrindo os 8 métodos do adapter HTTP.

### 2026-08-17 — Titular e Dependente ganham valor e vigência independentes no Período do Plano (`Claude/API (5).md`)

- **O que foi modificado:** numa sessão posterior (2026-08-17), sem nenhum commit do trabalho anterior, o usuário atualizou `Claude/API (5).md` — `valores_plano_saude` ganhou o campo `"tipo pessoa"` (`titular`/`dependente`): cada um passou a ter seu próprio período vigente e valor, em vez de compartilhar o mesmo `"valor"` do período (que era o modelo da rodada anterior, também desta sessão). Confirmado ao vivo contra o backend real (`GET /api/valores-plano-saude?filial=100`, leitura) antes de implementar — a filial 100 já tinha os 4 períodos (saúde/odontológico × titular/dependente) com o mesmo valor de antes, todos vigentes.
  - `PlanoSaudePeriodo` (`src/types/planoSaude.ts`) ganhou o campo `tipoPessoa: "titular" | "dependente"` — cada período agora é só de um tipo de pessoa, não mais compartilhado.
  - `PlanoSaudeService.salvarPeriodoPlanoSaude` passou a receber `tipoPessoa` como parâmetro; `encontrarPeriodoPlano` (`src/services/planoSaudeService.ts`) passou a filtrar também por `tipoPessoa` — como a "fechamento de período" agora vale por filial + tipo de plano + tipo de pessoa, continua havendo no máximo um período vigente por combinação, sem precisar de regra de desempate.
  - `LancamentoPlanoSaude.tsx`/`exportarPlanoSaudeExcel` (`planoSaudeService.ts`) passaram a chamar `encontrarPeriodoPlano` com `pessoa.tipo` ("titular"/"dependente" — já batia exatamente com o novo `tipoPessoa`) — Titular e Dependente voltam a poder ter valores diferentes na grade de Lançamento, agora batendo com o que a API real permite.
  - `src/adapters/mock/seed.ts`: cada filial/tipo de plano ganhou 2 períodos semeados (Titular e Dependente), com o mesmo valor de antes nos dois — mantém o comportamento default idêntico ao anterior.
  - `CadastroPeriodoPlano.tsx`: novo toggle **Titular/Dependente** (igual ao de Saúde/Odontológico) — cadastro, lista e "Encerrar vigência" agora operam sobre a combinação (tipo de plano, tipo de pessoa) selecionada.
  - `planoSaudeServiceHttp` (`src/adapters/http/planoSaudeService.http.ts`): `RespostaPeriodo`/`paraPeriodo` mapeando `"tipo pessoa"`; `POST /api/valores-plano-saude` passou a enviar `"tipo pessoa"` no corpo; `salvarPeriodoPlanoSaude` relista e filtra pelo `tipoPessoa` certo (podem existir 2 períodos vigentes simultâneos agora — um por tipo de pessoa — então não dá mais pra pegar só "o primeiro ativo").
- **Motivo:** pedido do usuário — ajustar as chamadas de Plano de Saúde conforme o novo documento de API, que passou a diferenciar valor/vigência de Titular e Dependente.
- **Impacto:** nenhuma mudança de contrato pra quem só lê a tela de Lançamento sem mexer em período (valores idênticos aos anteriores, já que o valor semeado é o mesmo pros dois tipos de pessoa). Testes atualizados: `CadastroPeriodoPlano.test.tsx` (toggle Titular/Dependente, período independente por tipo de pessoa, 8 casos), `planoSaudeService.http.test.ts` (corpo/resposta com `"tipo pessoa"`) — ver `Claude/testes/f5-plano-saude.md`. Verificado ao vivo (Playwright, só leitura): o toggle Titular/Dependente mostra o período vigente correto de cada um, batendo com o `GET /api/valores-plano-saude?filial=100` confirmado via `curl` — sem erros de console.

### 2026-08-17 — Período do Plano: sub-abas Titular/Dependente viram lista única + dois campos de valor

- **O que foi modificado:** o usuário pediu, ainda no mesmo dia e sem commit do trabalho anterior, pra não separar Titular e Dependente em sub-abas — em vez disso, uma lista só com uma coluna extra "Tipo de Pessoa" indicando de quem é o valor, e o formulário de cadastro com dois campos de valor (Valor Titular / Valor Dependente), podendo preencher só um ou os dois.
  - `CadastroPeriodoPlano.tsx`: removido o toggle Titular/Dependente (o estado `tipoPessoa` deixou de existir); a tabela volta a listar todos os `periodos` da filial/tipo de plano (sem o `.filter` por tipo de pessoa), com a nova coluna "Tipo de Pessoa" entre "Data de Início" e "Valor". O formulário passou a ter `novoValorTitular`/`novoValorDependente` em vez de um único `novoValor`.
  - `cadastrarPeriodo`: valida que pelo menos um dos dois campos foi preenchido (e que os preenchidos são válidos, `>= 0`); pra cada campo preenchido, chama `planoSaudeService.salvarPeriodoPlanoSaude(filial, tipoPlano, tipoPessoa, valor)` separadamente (a API não tem um endpoint que aceite os dois de uma vez — cada `tipoPessoa` é sempre um `POST` próprio) — sucessos parciais são possíveis (ex.: Titular já tem vigente e é recusado, mas Dependente não tem e é criado); erros de cada tentativa são concatenados num único toast, e só o(s) campo(s) que deram certo são limpos do formulário.
  - Nenhuma mudança em `PlanoSaudeService`/`planoSaudeService.mock.ts`/`planoSaudeService.http.ts` — a interface já era por `tipoPessoa` desde a rodada anterior; só a tela mudou.
- **Motivo:** pedido do usuário — preferência de UX por uma lista única com coluna identificadora em vez de abas separadas, e por poder cadastrar Titular e Dependente num único envio de formulário.
- **Impacto:** nenhum na API/mock. Testes de `CadastroPeriodoPlano.test.tsx` reescritos (9 casos): cobrem a lista combinada com a coluna nova, cadastro preenchendo só um campo, cadastro preenchendo os dois (dois períodos criados), rejeição por falta de preenchimento e rejeição por vigente duplicado — ver `Claude/testes/f5-plano-saude.md`. Verificado ao vivo (Playwright, servidor real, só leitura): a lista da filial 100 mostra as duas linhas (Titular/Dependente) juntas com a coluna nova, batendo com o `curl` feito antes — sem erros de console.

### 2026-08-17 — Descontos e Bonificações: coluna Total e rodapé Total geral saem da grade, viram modal "Totais por tipo"

- **O que foi modificado:** o usuário pediu pra retirar a coluna "Total" (por colaborador) e a linha "Total geral" da tela de Descontos/Bonificações, e criar um botão que abre uma modal com uma tabela de total por tipo de lançamento (ex.: "Bonificação R$ 300,00", "Multa -R$ 80,00") — só entram na modal os tipos que têm pelo menos um lançamento no mês.
  - `totaisPorTipo` (novo, `src/services/descontosService.ts`): para cada um dos 10 tipos do catálogo (`TIPOS_DESCONTO_BONIFICACAO`, em ordem fixa), filtra as linhas daquele tipo e, se houver pelo menos uma, calcula o total assinado reaproveitando `somarDescontosComSinal` (mesma regra: Bonificação/Ajuda de Custo somam, os demais subtraem) — tipos sem lançamento no mês não entram no resultado.
  - `Descontos.tsx`: removida a coluna "Total" da grade (cabeçalho, célula de cada colaborador, célula "R$ 0,00" da linha "Nenhum lançamento neste mês") e o `<tfoot>` "Total geral"; `colSpan` das linhas de estado vazio ajustado de 7→6. Novo botão "📊 Totais por tipo" (ao lado de "Exportar Excel da filial") abre uma `Modal` com uma `Table` de duas colunas (Tipo, Total), populada por `totaisPorTipo(linhas)` — recalculada a cada render a partir do estado local, então reflete edições ainda não salvas, igual o Total antigo fazia. Estado vazio (`LinhaVazia`) quando não há nenhum lançamento no mês.
  - **Correção no mesmo dia:** a tabela da modal usava a classe padrão `.tabela` (`min-width: 640px`) — mais larga que a área interna da modal (`width: min(92vw, 640px)` menos o padding de `.modal-conteudo`), então aparecia uma barra de rolagem horizontal cortando os valores da coluna Total. `Table` (`src/components/ui/Table.tsx`) ganhou uma prop `compacta` (nova classe `.tabela-compacta` em `Table.css`: `min-width: 0` + padding de célula reduzido de `12px var(--esp-4)` para `8px var(--esp-3)`) — reutilizável em qualquer tabela pequena dentro de modal; a modal de Descontos passou a usar `<Table compacta>`.
- **Motivo:** pedido do usuário — trocar um total por colaborador/geral (que não separa por tipo) por uma visão agregada por tipo de lançamento, sob demanda (numa modal) em vez de sempre visível na grade.
- **Impacto:** a regra de sinal (Bonificação/Ajuda de Custo somam, os demais subtraem) e a garantia de que o valor salvo nunca é negativo continuam as mesmas — só mudou onde esse total aparece. Testes de `Descontos.test.tsx` atualizados: os que dependiam do texto da coluna Total/rodapé (ex.: aguardar "150,00" aparecer na linha) foram trocados por asserções diretas no valor do campo (`toHaveValue`); o describe "Total com sinal" foi reescrito para abrir a modal e checar os totais por tipo lá, incluindo um caso novo (modal só lista tipos com lançamento, e mensagem de vazio quando não há nenhum) — ver `Claude/testes/f4-comissao-descontos.md`. Verificado ao vivo (Playwright, servidor real, dados reais da filial 100): a grade não tem mais coluna Total nem rodapé, e a modal abre com "Bonificação"/"Farmácia" mostrando os valores corretos — sem erros de console.
- **Correção no mesmo dia (mesma sessão, sem commit):** com os totais já separados por tipo na modal, o usuário pediu pra tirar a regra de sinal — todo tipo passou a somar positivo. `somarDescontosComSinal`/`TIPOS_QUE_SOMAM` (`src/services/descontosService.ts`) removidos por completo (não tinham mais nenhum outro uso no código, só dentro de `totaisPorTipo`); `totaisPorTipo` passou a somar `valor` puro por tipo, sem sinal. Testes ajustados: a linha "Multa"/"Farmácia" que antes esperava `/-R\$.../` passou a esperar o valor positivo puro, e um teste novo confirma que dois lançamentos do mesmo tipo (colaboradores diferentes) somam corretamente no total desse tipo.

### 2026-08-17 — Período do Plano: formulário inline vira botão "+ Novo período" com modal de 4 campos (`Claude/API (6).md`)

- **O que foi modificado:** o usuário abriu `Claude/API (6).md` (sem mudança na seção "Valores de Plano de Saúde" em relação a `API (5).md`) e pediu pra trocar o formulário inline (Valor Titular/Valor Dependente sempre visíveis acima da tabela) por um botão "+ Novo período" que abre uma modal com 4 campos: Data de Início, Valor Titular, Valor do Dependente e Data de Encerramento.
  - **Alinhado com o usuário antes de implementar** (a API não aceita nenhuma data na criação do período — `data_criacao` é sempre "agora", gerado pelo servidor): o usuário confirmou que o campo "Data de Início" deve existir só **de forma ilustrativa por enquanto** (a API vai ser adaptada no futuro pra aceitá-la) e que preencher "Data de Encerramento" deve **criar e encerrar o período na hora** (duas chamadas: `POST` seguido de `PUT .../encerrar`).
  - `PlanoSaudeService.encerrarPeriodoPlanoSaude` (`src/services/planoSaudeService.ts`) ganhou um 2º parâmetro opcional `dataValidade` — repassado pro corpo de `PUT /api/valores-plano-saude/{id}/encerrar` como `"data validade"` quando informado (a API já aceitava esse campo opcional desde sempre, só não tinha uso até agora); sem ele, continua exatamente como antes (corpo vazio, API assume hoje). Atualizado em `planoSaudeService.mock.ts` (usa a data informada em vez de sempre "hoje") e `planoSaudeService.http.ts`.
  - `CadastroPeriodoPlano.tsx`: o formulário deixou de ficar sempre visível — agora é `<Button>+ Novo período</Button>` que abre uma `Modal` com os 4 campos, na ordem pedida. "Data de Início" vem pré-preenchida com a data de hoje, editável, mas seu valor **não é enviado a lugar nenhum** — um texto de ajuda dentro da modal deixa isso explícito pro usuário. Ao salvar: pra cada campo de valor preenchido (Titular e/ou Dependente), chama `salvarPeriodoPlanoSaude`; se "Data de Encerramento" também estiver preenchida, chama `encerrarPeriodoPlanoSaude(periodoCriado, dataEncerramento)` logo em seguida — o período já aparece na lista como "Encerrado" com a data escolhida, sem precisar de uma 2ª ação manual. A modal fecha sozinha quando pelo menos um período é criado com sucesso; fica aberta se tudo falhar (ex.: os dois tipos de pessoa já têm período vigente).
- **Motivo:** pedido do usuário — preferência de UX (botão + modal, como já usado na modal "Totais por tipo" de Descontos) e capacidade de já cadastrar um período histórico (com encerramento) numa única ação.
- **Impacto:** nenhuma mudança de contrato pra quem só cadastra período vigente sem preencher "Data de Encerramento" (comportamento idêntico a antes). Testes de `CadastroPeriodoPlano.test.tsx` reescritos pra abrir a modal antes de preencher os campos (o botão que antes era "+ Novo período" e submetia o formulário agora só abre a modal; o botão de submeter dentro dela é "Adicionar"); casos novos cobrindo os 4 campos aparecendo na modal, criar+encerrar em uma ação, e a Data de Início não ter efeito no cadastro. `planoSaudeService.http.test.ts` ganhou um caso novo confirmando que `"data validade"` é enviada no corpo do `PUT .../encerrar` quando informada — ver `Claude/testes/f5-plano-saude.md`. Verificado ao vivo (Playwright, servidor real, filial 100, só leitura — sem submeter o formulário contra a API real): a modal abre com os 4 campos na ordem certa, "Data de Início" pré-preenchida com hoje, texto de ajuda visível, sem erros de console.

### 2026-08-17 — Data de Início do período do plano vira retroativa de verdade (`Claude/API (7).md`)

- **O que foi modificado:** o usuário abriu `Claude/API (7).md`, que trouxe `"data inicio"` editável (podendo ser retroativa) e `"data fim"` opcional no `POST/PUT /api/valores-plano-saude` — exatamente o que faltava pra tornar real o campo "Data de Início" que até então era só ilustrativo (ver entrada anterior, mesmo dia). Pedido do usuário: atualizar a chamada de cadastro pra usar isso.
  - `PlanoSaudePeriodo` (`src/types/planoSaude.ts`) ganhou o campo `dataInicio` (`"data inicio"` da API — quando o período passa a valer, pode ser retroativa), agora **separado** de `dataCriacao` (que continua sendo só o timestamp de quando o registro foi criado no sistema — os dois podem divergir quando a data de início é retroativa).
  - `encontrarPeriodoPlano` (`src/services/planoSaudeService.ts`) passou a comparar por `dataInicio` em vez de `dataCriacao` pra decidir qual período vale num mês — é essa mudança que faz a retroatividade ter efeito real na aba Lançamento, não só na lista da tela de Período. Como um período histórico já pode nascer com `dataFim` no cadastro (sem depender da ordem de criação), a busca percorre a lista de trás pra frente e usa o mais recentemente cadastrado em caso de sobreposição (raro, só se o Admin cadastrar dois períodos históricos com intervalos que se cruzam por engano).
  - `PlanoSaudeService.salvarPeriodoPlanoSaude` ganhou dois parâmetros opcionais a mais, `dataInicio` e `dataFim`. `planoSaudeService.mock.ts`: `dataInicio` vai direto pro registro (padrão hoje se omitida); com `dataFim`, o período já nasce `ativo: false` com `dataValidade` preenchida, e a checagem de "já existe vigente" é pulada nesse caso (replicando a exceção documentada na API). `planoSaudeService.http.ts`: `POST` passou a enviar `"data inicio"`/`"data fim"` quando informadas; como agora pode existir mais de um período `ativo: false` recém-criado pro mesmo tipo de pessoa, a busca pós-`POST` (a API não devolve o registro criado) passou a casar por `valor`+`dataValidade` nesse caso, em vez de simplesmente pegar "o vigente".
  - `CadastroPeriodoPlano.tsx`: **removida a lógica de 2 chamadas** (criar + `encerrarPeriodoPlanoSaude` em seguida) — "Data de Início" e "Data de Encerramento" agora vão os dois no mesmo `salvarPeriodoPlanoSaude(...)`, um único `POST`. Texto de ajuda da modal atualizado (não fala mais em "campo ilustrativo"). Validação nova no front: recusa se a Data de Encerramento for anterior à Data de Início (mesma regra que a API aplica com `400`). A coluna "Data de Início" da lista passou a mostrar `periodo.dataInicio` (não mais `periodo.dataCriacao`).
- **Motivo:** pedido do usuário — permitir cadastrar/corrigir um período retroativamente, agora que a API suporta isso de verdade.
- **Impacto:** comportamento idêntico a antes pra quem não mexe na Data de Início (continua "hoje" por padrão). Períodos semeados (`src/adapters/mock/seed.ts`) ganharam `dataInicio: "2000-01-01"`, igual à `dataCriacao` que já tinham — sem mudança visível. Testes: `planoSaudeService.http.test.ts` ganhou 2 casos novos (retroativa enviada corretamente; `dataFim` cria já histórico e é encontrado certo ao relistar mesmo com um período vigente concorrente na lista) — 13 testes no total, todos passando. `CadastroPeriodoPlano.test.tsx`: o teste que antes provava que a Data de Início "não tinha efeito" foi reescrito pra provar o oposto (período retroativo aparece na lista com a data escolhida), e ganhou um caso novo de rejeição quando Data de Encerramento < Data de Início — ver `Claude/testes/f5-plano-saude.md`. Verificado ao vivo (Playwright, servidor real, filial 100, só leitura — sem submeter o formulário contra a API real, por não ter autorização de escrita nesta sessão): a modal abre com o texto de ajuda atualizado, sem erros de console.

### 2026-08-17 — Dependentes ganham checkboxes próprias de adesão Saúde/Odontológico (F5.PS-CAD-06)

- **O que foi modificado:** na mesma leva de pedidos do dia, o usuário pediu pra trocar o ✓/— (texto, só leitura, espelhando sempre a adesão do titular) por checkboxes de verdade — marcáveis/desmarcáveis — pros dependentes, na tela "Titulares e Dependentes".
  - **Alinhado com o usuário antes de implementar:** a API de Dependentes (`Claude/API (7).md`, `GET/POST/PUT /api/dependentes`) não tem nenhum campo de adesão — só `nome`/`cpf`. O usuário confirmou (a) que isso deve funcionar **só local por enquanto** (mesmo padrão já usado pra "Data de Início" antes da API aceitar — ver entrada acima), até a API ganhar o campo, e (b) que desmarcar a adesão de um dependente deve **tirá-lo da grade de Lançamento** daquele tipo de plano, mesmo com o titular aderido — mesma lógica que já existia pro titular, agora por pessoa.
  - `PlanoSaudeDependente` (`src/types/planoSaude.ts`) ganhou `adesaoSaude?`/`adesaoOdontologico?` (default aderido quando `undefined`, mesma convenção do titular).
  - `PlanoSaudeService` ganhou `salvarAdesaoDependente(dependenteId, tipo, valor)`. `planoSaudeService.mock.ts`: persiste de verdade (grava no `localStorage`, mesmo padrão de tudo mais no mock). `planoSaudeService.http.ts`: **não chama nenhum endpoint** (não existe um pra isso) — só resolve sucesso na hora, e o estado atualizado fica só na tela (React), perdido ao recarregar, até a API ganhar suporte.
  - `listarPessoasPlanoSaude` (`src/services/planoSaudeService.ts`) passou a filtrar também pela adesão do próprio dependente (além da do titular) — um dependente desmarcado não entra mais na grade de Lançamento daquele tipo de plano, mesmo com a família toda aderida.
  - `CadastroTitulares.tsx`: linha do dependente trocou as células de texto `✓`/`—` por `<input type="checkbox">` de verdade, com `aria-label` própria (`Plano de Saúde de <nome>`/`Plano Odontológico de <nome>`), desabilitadas pra quem não é Admin — mesmo padrão visual/permissão do titular.
- **Motivo:** pedido do usuário — permitir que a família tenha adesão mista (ex.: titular com Plano de Saúde, mas um dependente específico sem), coisa que a UI anterior não deixava expressar (o dependente sempre herdava a adesão do titular, sem opção).
- **Impacto:** nenhuma mudança pra famílias onde ninguém mexeu no checkbox do dependente (continua aderido por padrão, aparecendo na grade normalmente). Testes: `CadastroTitulares.test.tsx` — o teste que provava "adesão do dependente espelha o titular" foi reescrito pra provar o oposto (checkbox própria, marcada por padrão); teste novo de desmarcar a adesão de um dependente sem afetar o titular; o teste de "Coordenador vê tudo desabilitado" passou a cadastrar um dependente primeiro (como Admin) e conferir que o checkbox dele também fica `disabled` pro Coordenador — 7 testes no total. `LancamentoPlanoSaude.test.tsx` ganhou um caso novo: dependente com adesão própria desmarcada não aparece na grade mesmo com o titular aderido (usa o mock adapter direto pra montar o cenário, sem depender da tela de Titulares). `planoSaudeService.http.test.ts` ganhou um caso confirmando que `salvarAdesaoDependente` nunca chama `fetch` — ver `Claude/testes/f5-plano-saude.md`. Verificado ao vivo (Playwright, servidor real, filial 100): já existia um dependente real ("teste dependente") na filial — cliquei no checkbox de Plano de Saúde dele e desmarcou visualmente, sem afetar o checkbox do titular e sem nenhuma chamada de rede (consistente com o no-op do HTTP); nenhum erro de console. Como o valor não é persistido em lugar nenhum no backend real, um recarregamento da página volta a mostrar como aderido — comportamento esperado e não uma regressão.

### 2026-08-18 — Adesão do dependente passa a persistir de verdade (`Claude/API (8).md`) + sinalização de "salvando" nos checkboxes

- **O que foi modificado:** `Claude/API (8).md` trouxe `"plano saude"`/`"plano odontologico"` em `POST`/`PUT /api/dependentes/{id}` — o campo que faltava pra tornar real a adesão do dependente (entrada anterior, 2026-08-17, ainda local-only). Pedido do usuário: ligar o cadastro de verdade e sinalizar visualmente quando um checkbox (titular ou dependente) está sendo salvo no banco.
  - `planoSaudeServiceHttp.salvarAdesaoDependente` (`src/adapters/http/planoSaudeService.http.ts`) deixou de ser um no-op — agora chama `PUT /api/dependentes/{id}` com só o campo do tipo alterado (`"plano saude"` ou `"plano odontologico"`), mesmo padrão já usado em `salvarAdesao` (titular) contra `PUT /api/usuarios/{id}`.
  - Comentários desatualizados removidos: `PlanoSaudeDependente.adesaoSaude`/`adesaoOdontologico` (`src/types/planoSaude.ts`) e a assinatura de `salvarAdesaoDependente` em `PlanoSaudeService` (`src/services/planoSaudeService.ts`) não falam mais em "só local" — persiste de verdade nos dois adapters agora (mock já persistia desde a entrada anterior).
  - `CadastroTitulares.tsx`: cada checkbox de adesão (titular **e** dependente, Saúde e Odontológico) ganhou um indicador visual ao lado — um spinner (reaproveitando `.spinner-botao`, já usado nos botões com `carregando`) + texto "Salvando…" — visível enquanto aquele clique específico está em voo (`alternandoAdesao` já existia pra desabilitar o checkbox durante o salvamento; a UI antes não deixava isso visível, só o cursor/disabled). Nova classe `.indicador-salvando` em `Table.css`.
- **Motivo:** pedido do usuário, seguindo o mesmo padrão já usado quando "Data de Início" ganhou suporte real da API (entrada de 2026-08-17) — completar a persistência assim que a API ganha o campo que faltava, e dar um sinal visual explícito de que a alteração está sendo gravada no banco.
- **Impacto:** nenhuma mudança de comportamento pra quem só lê a tela — a adesão do dependente agora sobrevive a um recarregamento de página (antes voltava ao estado anterior, por não ser persistida no backend real). Testes: `planoSaudeService.http.test.ts` — o caso que provava "não chama a API, no-op" foi reescrito pra provar o oposto (`PUT /api/dependentes/{id}` com só o campo alterado), mesmo padrão do teste já existente de `salvarAdesao`. Suíte completa: 253 testes, 253 passaram (sem mudança na contagem — o teste foi reescrito, não adicionado); o indicador visual "Salvando…" não ganhou um teste dedicado por ser um estado transiente (só existe entre o clique e a resposta), mas não quebrou nenhum teste existente do checkbox. `npx tsc -b --noEmit`, `npx oxlint` e `npm run build` sem erros. **Não testado ao vivo contra o backend real nesta rodada** — agora que o clique grava de verdade no banco, deixou de ser seguro testar sem autorização explícita de escrita (diferente da rodada anterior, que era comprovadamente um no-op); recomenda-se confirmar visualmente numa próxima sessão com autorização de escrita.

### 2026-08-18 — Lançamento: rodapé ganha Total ativos/Total desligados/Total geral (F5.PS-LAN-05b, `Claude/API (8).md`)

- **O que foi modificado:** pedido do usuário, fora do `ROADMAP.md` original (que só previa um único rodapé de totais) — criar duas linhas acima do total geral: **Total ativos** (o que já era calculado, renomeado) e **Total desligados** (valor único, não itemizado por colaborador desligado, com campo aberto só pra preenchimento do total). **Total geral** passou a ser Total ativos + Total desligados. Decisão de escopo confirmada com o usuário antes de implementar: **Total desligados ficou editável nas duas sub-abas** (Saúde e Odontológico), não só em Saúde — a API aceita `"tipo plano": saude | odontologico` no endpoint novo, então restringir a Saúde deixaria o valor de Odontológico sem como ser cadastrado por esta tela.
  - `PlanoSaudeService.listarLancamentosPlanoSaude` (`src/services/planoSaudeService.ts`) mudou de retornar `PlanoSaudeLancamento[]` pra `{ lancamentos: PlanoSaudeLancamento[]; totalDesligados: number }` — o total de desligados já vem no mesmo `GET /api/lancamentos` (`"total desligados"`, `Claude/API (8).md`), então reaproveitar essa mesma chamada evita uma requisição a mais. Novo método `salvarTotalDesligadosPlanoSaude(filial, mesReferencia, tipoPlano, valor)`.
  - `planoSaudeService.mock.ts`: nova coleção `planoSaudeDesligados` (por filial + tipo de plano + mês); em `filial=Todas`, soma os registros de todas as filiais (mesma regra documentada pra API real). `salvarTotalDesligadosPlanoSaude` recusa se a filial for "Todas" (só dá pra gravar numa filial específica).
  - `planoSaudeService.http.ts`: `RespostaLancamentos` ganhou `"total desligados"`; `salvarTotalDesligadosPlanoSaude` chama `PUT /api/lancamentos/desligados` com `filial`/`"tipo plano"`/`"mes de referencia"`/`valor`. O relist interno de `salvarLancamentoPlanoSaude` (que usa a mesma função) foi ajustado pro novo formato de retorno.
  - `LancamentoPlanoSaude.tsx`: rodapé passou de 1 pra 3 linhas — Total ativos (mesmas colunas de antes), Total desligados (colunas Titular/Dependente/Adicional/Coopart. mostram "—"; só a coluna Total tem um `<input>` numérico, `aria-label="Total de desligados"`) e Total geral (mesmo padrão de "—", só o Total final soma os dois). O campo de desligados só é editável numa filial específica (`!mostrarFilial`) — em "Todas as filiais" fica `disabled`, mostrando o valor agregado só pra leitura. `salvar()` passou a fazer duas coisas quando aplicável: salvar os lançamentos por pessoa (só Saúde, só se houver pessoas) e salvar o total de desligados (se houver filial específica) — as duas no mesmo clique do botão "💾 Salvar lançamento do mês".
  - Consequência direta da decisão acima: o botão "💾 Salvar lançamento do mês" e o botão de bloqueio (`🔒`) passaram a aparecer também em Odontológico + filial específica (antes escondidos, já que nada era editável ali) — o bloqueio já é por tela `planoSaude` (não por tipo de plano), então destravar/travar continua valendo pros dois tipos ao mesmo tempo.
- **Motivo:** pedido do usuário, junto com o novo módulo `PUT /api/lancamentos/desligados` do `Claude/API (8).md` — separar o custo de quem já saiu da empresa (hoje um valor único que a filial paga, sem itemizar por pessoa) do total de quem está ativo, sem precisar lançar um "colaborador fantasma" por desligado.
- **Impacto:** nenhuma mudança de contrato pra quem não mexe no campo de desligados (Total geral = Total ativos, igual antes, já que `totalDesligados` some `0` quando nada foi cadastrado). Testes: `planoSaudeService.http.test.ts` — teste de `listarLancamentosPlanoSaude` atualizado pro novo formato de retorno (`lancamentos`/`totalDesligados`); caso novo de `salvarTotalDesligadosPlanoSaude` (14 testes no total). `LancamentoPlanoSaude.test.tsx` — teste de Odontológico reescrito (não espera mais o botão ausente); casos novos: as três linhas do rodapé aparecem e o geral soma ativos+desligados; campo desabilitado em "Todas as filiais"; botão de bloqueio também aparece em Odontológico (11 testes no total). Suíte completa: 257 testes, 257 passaram. `npx tsc -b --noEmit`, `npx oxlint` e `npm run build` sem erros. Verificado ao vivo (Playwright, servidor real, filial 100, só leitura — sem clicar em "Salvar", já que agora grava de verdade e não há autorização de escrita nesta sessão): as três linhas aparecem certas nas duas sub-abas, o botão de bloqueio e o de salvar aparecem em Odontológico também, campo de desligados vazio e editável; sem erros de console.

### 2026-08-18 — Colaboradores: botões "Editar"/"Remover" viram menu "⋮" com "Resetar senha" nova (F2.CAD-04)

- **O que foi modificado:** pedido do usuário, fora do `ROADMAP.md` original — na tela de Cadastro de Colaboradores, os dois botões da coluna de Ações ("Editar"/"Remover", visíveis só pro Admin) viraram um ícone de três pontos verticais ("⋮") que abre um menu com três ações: **Editar** (comportamento idêntico a antes), **Resetar senha** (nova) e **Remover** (comportamento idêntico a antes).
  - Componente novo e genérico `MenuAcoes` (`src/components/ui/MenuAcoes.tsx` + `.css`) — botão "⋮" que abre uma lista de itens (`role="menu"`/`"menuitem"`), fecha com Escape, clique fora, ou rolagem/redimensionamento da janela. Renderizado via `createPortal` em `document.body` (mesmo padrão do `Modal`) e posicionado com `position: fixed` calculado a partir do `getBoundingClientRect()` do botão — necessário porque `.tabela-wrapper` tem `overflow-x: auto` (que por herança do CSS também vira `overflow-y: auto`), então um menu posicionado `absolute` dentro da tabela seria cortado.
  - **Resetar senha:** reaproveita o `salvarColaborador` já existente (`PUT /api/usuarios/{id}` com o colaborador inteiro, só trocando `senhaAcesso` pelo próprio `cpf`) — nenhum método novo no serviço. A API já documenta (`Claude/API (8).md`) que enviar `"senha"` força `"precisa trocar senha": true` automaticamente, exatamente o comportamento pedido (colaborador é obrigado a trocar a senha no próximo acesso) — não precisou enviar esse campo explicitamente.
  - Sem modal de confirmação pra "Resetar senha" nem "Remover" — mantém o mesmo padrão que "Remover" já tinha (ação imediata + toast de sucesso/erro), sem introduzir uma fricção nova que o app não usa em nenhum outro lugar.
- **Motivo:** pedido do usuário — reduzir a quantidade de botões visíveis na tabela e dar ao Admin uma forma de resetar o acesso de um colaborador sem precisar saber/redefinir uma senha manualmente (usa o CPF, que o Admin já tem à mão na própria tabela).
- **Impacto:** nenhuma mudança de contrato com a API. `CadastroColaboradores.test.tsx` — os testes de "Editar"/"Remover" foram adaptados pra abrir o menu antes (`getByRole("button", { name: "Mais ações de <nome>" })` → `getByRole("menuitem", ...)`); casos novos: "Resetar senha" chama `salvarColaborador` com `senhaAcesso` igual ao CPF da linha; o menu fecha com Escape e com clique fora (16 testes no total, antes 14). **Bug pego e corrigido durante a escrita dos testes, antes de qualquer commit:** o listener de "clique fora" do `MenuAcoes` só considerava o botão "⋮" como "dentro" do menu — clicar em qualquer item da lista (que é renderizada em outro lugar da árvore, via portal) contava como "fora" e fechava o menu no `mousedown`, antes do `click` do item disparar, cancelando a ação. Corrigido adicionando uma segunda ref (a própria lista) à checagem. Suíte completa: 264 testes, 264 passaram. `npx tsc -b --noEmit`, `npx oxlint` e `npm run build` sem erros. **Não verificado ao vivo contra o backend real nesta rodada:** a tentativa de verificação (Playwright, só leitura — abrir o menu e clicar em "Editar" pra depois cancelar) esbarrou num problema externo ao app: o login do usuário `admin` no backend real passou a responder `404 {"mensagem":"usuario nao encontrado"}` no meio da sessão de testes (confirmado via `curl` direto, sem passar pelo front-end), enquanto o login de `gerente` continuou funcionando normalmente — não foi causado por nenhuma ação desta sessão (nenhuma chamada de escrita chegou a ser feita contra o colaborador `admin`; a falha aconteceu antes de qualquer clique em "Resetar senha"/"Remover"). Recomenda-se ao usuário verificar o cadastro do usuário `admin` no backend numa próxima sessão.

### 2026-08-18 — Colaboradores: menu "⋮" ganha "Ativar"/"Inativar colaborador" (F2.CAD-04)

- **O que foi modificado:** pedido do usuário, seguindo o mesmo padrão da entrada anterior (mesmo dia) — o menu "⋮" da tela de Cadastro de Colaboradores ganhou uma quarta ação, que alterna entre "Inativar colaborador" e "Ativar colaborador" dependendo do status atual. Nova coluna **Status** na tabela, com um selo (`Selo`, componente já existente em `src/components/ui/Badge.tsx` mas sem nenhum uso até agora) — verde "Ativo" ou vermelho "Inativo".
  - `Colaborador` (`src/types/colaborador.ts`) ganhou `desligado?: boolean` (`undefined`/`false` = ativo, `true` = inativo — mesma convenção de default já usada em `adesaoSaude`/`adesaoOdontologico`). Campo já documentado desde `Claude/API (8).md` (`colaboradores.desligado`) mas nunca tinha sido modelado no front.
  - `colaboradoresServiceHttp` (`src/adapters/http/colaboradoresService.http.ts`): `RespostaUsuario`/`paraColaborador` passaram a mapear `desligado`; `salvarColaborador` passou a enviar `desligado: colaborador.desligado ?? false` no corpo do `PUT`/`POST` (igual já fazia com `"plano saude"`/`"plano odontologico"`).
  - `alternarStatus` (`CadastroColaboradores.tsx`) reaproveita `salvarColaborador` — sem endpoint novo, mesma ideia de "Resetar senha". Marcar `desligado: true` já revoga todos os tokens do colaborador na hora e bloqueia login futuro, conforme documentado desde `Claude/API (8).md`/`(9).md`.
  - **Bug pego e corrigido antes de qualquer commit, sem precisar do usuário reportar:** o formulário de Editar (`FormularioColaborador`) nunca teve campo `desligado` — `tratarSubmit` montava o corpo só com os campos do formulário mais `id`, então editar qualquer outro campo (nome, cargo, etc.) de um colaborador já inativado reenviaria `desligado: false` por padrão (via o `?? false` do adapter HTTP) e **reativaria o colaborador em silêncio**. Corrigido: `tratarSubmit` agora busca o colaborador original em `lista` e preserva o `desligado` atual dele no envio, independente do que o formulário (que não expõe esse campo) contém.
- **Motivo:** pedido do usuário — dar ao Admin uma forma de suspender o acesso de um colaborador (ex.: afastamento, desligamento em análise) sem precisar excluir o cadastro (que apagaria o histórico) nem trocar a senha manualmente.
- **Impacto:** nenhuma mudança de contrato com a API. Testes: `colaboradoresService.http.test.ts` — 2 casos novos (mapeia `desligado` na leitura; envia `desligado: true` na escrita) — 8 testes no total. `CadastroColaboradores.test.tsx` — 3 casos novos (inativar grava `desligado: true` e o rótulo do menu vira "Ativar colaborador"; ativar de volta grava `false`; editar outros campos de um colaborador inativado não o reativa — cobre o bug acima) — 19 testes no total. Suíte completa: 269 testes, 269 passaram. `npx tsc -b --noEmit`, `npx oxlint` e `npm run build` sem erros. **Não verificado ao vivo contra o backend real nesta rodada** — mesmo bloqueio já registrado na entrada anterior (login de `admin` retornando `404` no backend real, sem relação com este app).

### 2026-08-18 — Login redireciona pra troca de senha obrigatória quando `"precisa trocar senha"` vem `true` (F2.AUTH-02)

- **O que foi modificado:** pedido do usuário — se o colaborador loga e a API sinaliza `"precisa trocar senha": true` (acesso recém-criado, ou senha resetada pelo Admin via "Resetar senha", entrada anterior), em vez de cair no Shell normal ele cai numa tela que só permite definir uma senha nova; depois de trocada, desloga e volta pra tela de login.
  - **Alinhado com o usuário antes de implementar:** a API real não tem endpoint de autoatendimento pra o colaborador trocar a própria senha — `PUT /api/usuarios/{id}` é **só Admin** (`Claude/API (8).md`/`(9).md`, "ainda não existe endpoint de autoatendimento... por ora só o Admin zera manualmente"). Como a maioria de quem recebe essa flag não é Admin (colaborador novo ou resetado pelo Admin, tipicamente Vendedor/Gerente/Coordenador), implementar a chamada mesmo assim significa que ela só funciona de verdade hoje quando quem está logado é o próprio Admin — pra todo o resto, a API recusa com `403`. Confirmado com o usuário: implementar mesmo assim (mesmo padrão já usado antes neste projeto pra recursos que o front adianta e a API ainda não alcançou, ex.: Data de Início do Período do Plano), com uma mensagem de erro clara em vez de deixar o usuário preso numa tela sem explicação.
  - `Sessao` (`src/types/auth.ts`) ganhou `idColaborador?: string` (id do colaborador logado, em qualquer papel — antes só existia `vendedorId`, que a HTTP já preenchia sempre mas o nome/comentário sugeria só pra vendedor) e `precisaTrocarSenha?: boolean`.
  - `AuthService` ganhou `trocarSenhaPropria(idColaborador, novaSenha)`. `authServiceHttp`: `PUT /api/usuarios/{id}` com `{ senha, "precisa trocar senha": false }` — o `false` explícito é obrigatório porque a API já documenta que enviar `senha` sozinha força a flag de volta pra `true`; erro `403` vira uma mensagem específica ("Autoatendimento... ainda não está disponível para o seu perfil. Peça para o Administrador redefinir sua senha") em vez da mensagem genérica da API. `authServiceMock`: atualiza `senhaAcesso`/zera `precisaTrocarSenha` no colaborador (mock não modela o 403 — o bloqueio é só do backend real). `Colaborador` ganhou `precisaTrocarSenha?: boolean` (mock-only, pra poder simular o cenário em teste).
  - `SessaoContext` ganhou a action `trocarSenha(novaSenha)` — chama `trocarSenhaPropria` e, se der certo, chama `sair()` na hora (desloga, volta pro login); mantém a lógica de "depois de trocar volta pro login" centralizada ali em vez de espalhada pela tela.
  - `TrocarSenhaObrigatoria.tsx` (nova, `src/views/auth/`) — reaproveita o visual da tela de Login (`Login.css`, mesmas classes) e o `FloatingField`/mostrar-ocultar senha; dois campos (Nova senha, Confirmar), valida preenchimento e que as duas batem antes de chamar `trocarSenha`. Ícone de olho (`IconeOlho`) extraído de `Login.tsx` pra `src/components/ui/IconeOlho.tsx`, reutilizado nas duas telas em vez de duplicado.
  - `App.tsx`: passou de um `if/else` (Login × Shell) pra três estados — sem sessão → Login; sessão com `precisaTrocarSenha` → `TrocarSenhaObrigatoria`; sessão normal → Shell.
- **Motivo:** pedido do usuário — fechar o ciclo da flag `"precisa trocar senha"`, que a API já expõe desde antes mas o front nunca lia.
- **Impacto:** nenhuma mudança de contrato pra sessões sem a flag (segue caindo no Shell direto, igual antes). Testes: `authService.http.test.ts` — 2 casos novos (mapeia `precisaTrocarSenha` no login; `trocarSenhaPropria` envia o corpo certo e trata 403) — fixtures existentes atualizadas pra incluir `idColaborador`. `TrocarSenhaObrigatoria.test.tsx` (novo, 4 testes) e `App.test.tsx` (novo, 3 testes) cobrindo o fluxo ponta a ponta via mock (login com a flag → tela de troca → salvar → volta pro login com a senha nova valendo). Suíte completa: 279 testes, 279 passaram. `npx tsc -b --noEmit`, `npx oxlint` e `npm run build` sem erros.
- **Não verificado ao vivo contra o backend real nesta rodada, e um novo problema externo encontrado:** além do login de `admin` continuar respondendo `404` (já registrado), o login de `gerente` — que funcionava normalmente até a entrada anterior desta mesma sessão — passou a responder `403 {"mensagem": "colaborador desligado da empresa - acesso bloqueado"}`. Nenhuma ação desta sessão mudou o cadastro de `gerente` (a funcionalidade de Ativar/Inativar da entrada anterior nunca chegou a ser clicada ao vivo — ficou só na suíte automatizada, pela mesma falta de autorização de escrita). Registrado aqui pra rastreabilidade; recomenda-se ao usuário investigar o estado das contas `admin`/`gerente` no backend real.

### 2026-08-18 — `Claude/API (10).md` resolve as duas pendências da rodada anterior: troca de senha vira autoatendimento de verdade, e Total desligados passa a persistir por coluna

- **O que foi modificado:** pedido do usuário, seguindo a mesma sessão — `Claude/API (10).md` trouxe duas mudanças que impactam direto features implementadas hoje mesmo, mais cedo:
  1. **`PUT /api/trocar-senha`** (novo) — "o colaborador troca a própria senha (exige a senha atual), zerando 'precisa trocar senha' automaticamente", sem exigência de papel. Isso substitui a versão anterior (`PUT /api/usuarios/{id}`, só-Admin) que só funcionava de verdade pro próprio Admin.
  2. **`PUT`/`GET /api/lancamentos/desligados`** — o `valor` único virou `"valor titular"`/`"valor dependente"`/`"valor adicional"`/`"valor coparticipacao"` (mesma composição da grade normal); `GET /api/lancamentos` também passou a devolver os quatro campos agregados (em vez de só `"total desligados"` somado). Isso resolve o trade-off "quebra por coluna não sobrevive a um recarregamento" registrado na entrada de hoje mais cedo.
  - **Troca de senha:** `AuthService.trocarSenhaPropria` mudou de assinatura — `(idColaborador, novaSenha)` virou `(senhaAtual, novaSenha)`, já que o novo endpoint identifica "o colaborador autenticado" pelo token, não por id explícito. `authServiceHttp` chama `PUT /api/trocar-senha` com `{"senha atual", "senha nova"}`; erro `403` agora significa "senha atual incorreta" (validação normal), não mais falta de permissão — a mensagem especial "peça pro Administrador" foi removida por estar errada nesse novo cenário. `authServiceMock` passou a de fato validar a senha atual (antes não validava nada) — como o mock não tem sessão/token, guarda o id de quem logou por último numa chave própria (`authMockColaboradorLogado`) só pra saber em quem mexer, do mesmo jeito que o backend faria via token. `TrocarSenhaObrigatoria.tsx` ganhou um 3º campo, "Senha atual" (antes da Nova senha), com o mesmo mostrar/ocultar. `Sessao.idColaborador` (que só existia pra sustentar a versão anterior da chamada) foi removido — não faz mais falta.
  - **Total desligados:** `TotaisDesligadosPlano` (novo tipo, `src/types/planoSaude.ts`) — `{ titular, dependente, adicional, coparticipacao }`. `PlanoSaudeService.listarLancamentosPlanoSaude`/`salvarTotalDesligadosPlanoSaude` passaram a usar esse tipo em vez de `number`. `LancamentoPlanoSaude.tsx` simplificou bastante: o estado que antes era dividido em dois (`desligadosPorColuna`, sempre zerado ao carregar, e `totalDesligadosValor`, o único valor que de fato vinha do backend) virou um estado só, carregado direto da API — a coluna Total da linha e a linha "Total geral" passaram a ser sempre calculadas a partir dele, sem precisar mais rastrear o total separadamente. O aviso de "os campos voltam a zero ao recarregar" foi removido do texto da tela, porque deixou de ser verdade.
- **Motivo:** a API alcançou, no mesmo dia, duas limitações que tinham acabado de ser contornadas ("implementa mesmo que só funcione pro Admin" / "aceita que a quebra por coluna se perca ao recarregar") — ajustar pra usar o caminho de verdade assim que ele apareceu, em vez de deixar o front preso na versão provisória.
- **Impacto:** troca de senha passa a funcionar pra qualquer papel (antes só Admin). Total desligados sobrevive a um recarregamento (antes só o total agregado voltava, os 4 campos zeravam). Testes: `authService.http.test.ts` — fixtures de login sem mais `idColaborador`; `trocarSenhaPropria` reescrito pro novo endpoint/corpo/mensagem de erro. `authService.mock` — teste de rejeição por senha atual incorreta, novo. `TrocarSenhaObrigatoria.test.tsx` — preenche o campo novo em todos os testes que submetem o formulário; caso novo de senha atual incorreta. `planoSaudeService.http.test.ts`/`planoSaudeService.mock` — fixtures e asserções atualizadas pro objeto `TotaisDesligadosPlano` em vez de `number`. `LancamentoPlanoSaude.test.tsx` ganhou um caso novo provando a persistência: salva com valores diferentes por coluna, desmonta e remonta a tela, confere que os campos voltam preenchidos (antes essa mesma sequência provava o oposto). Suíte completa: 281 testes, 281 passaram. `npx tsc -b --noEmit`, `npx oxlint` e `npm run build` sem erros. Verificado ao vivo (Playwright, servidor real, filial 100, Saúde e Odontológico, só leitura — sem clicar em "Salvar senha" nem "Salvar lançamento do mês", por não ter autorização de escrita): a coluna "Total desligados — Titular" carregou "3800" de um lançamento real já salvo antes dessa mudança (prova que a leitura do novo formato funciona contra dados reais); nenhum erro de console. **Login de `admin` voltou a funcionar** nesta verificação (diferente da rodada anterior, quando dava `404`) — segue sem explicação, questão externa ao app.

### 2026-08-18 — Total desligados: relato do usuário ("não está mais editável") era o comportamento esperado; três ajustes de UX no mesmo fluxo

- **O que foi modificado:** usuário relatou que os campos de Total desligados pareciam não estar mais editáveis. Investigado ao vivo (Admin, filial 100, real) antes de mexer em qualquer código: os campos funcionavam normalmente (editáveis, digitação persistindo) — perguntado ao usuário, confirmou que estava em "Todas as filiais" no momento (Admin sempre entra assim por padrão) — comportamento já documentado (entrada de hoje mais cedo), não um bug. A partir disso, três ajustes pedidos pro mesmo fluxo:
  1. **Layout:** os 4 campos da linha Total desligados ganharam texto dourado/negrito (`--cor-accent-text`, mesmo token já usado nas células de Total) — nova classe `.celula-input-dourado` em `Table.css`, pra destacar visualmente que são campos de total, iguais ao padrão já usado no resto da grade.
  2. **Valor padrão:** os 4 campos mostravam vazio (`""`) quando o valor era `0` (`value={x || ""}`); passaram a mostrar `0` de verdade (`value={x}`), pra não parecer "sem valor nenhum" quando na verdade é zero.
  3. **Aviso:** novo texto abaixo da tabela, visível só quando os campos estão desabilitados por estar em "Todas as filiais" — explica que é preciso selecionar uma filial específica no cabeçalho pra poder editar, em vez de deixar o campo desabilitado sem explicação (foi exatamente essa falta de explicação que gerou o relato inicial do usuário).
- **Motivo:** pedido do usuário, depois de esclarecido que o comportamento original já era o esperado — tornar mais claro visualmente o que é um campo de total e por que ele às vezes aparece desabilitado.
- **Impacto:** nenhuma mudança de comportamento/contrato — só visual e de clareza. Testes: `LancamentoPlanoSaude.test.tsx` — o teste de "Todas as filiais" ganhou a asserção do texto de aviso; caso novo confirmando que os campos mostram `0` (não vazio) numa filial específica sem lançamento de desligados (13 testes no total, 12 antes). Suíte completa: 282 testes, 282 passaram. `npx tsc -b --noEmit`, `npx oxlint` e `npm run build` sem erros. Verificado ao vivo (Playwright, servidor real, filial 100 e "Todas as filiais", Admin, só leitura): texto dourado visível nos 4 campos; valores zerados mostram "0"; aviso aparece só em "Todas as filiais" e some ao trocar pra uma filial específica. Nenhum erro de console.

### 2026-08-18 — Colaboradores: barra de busca (F2.CAD-03)

- **O que foi modificado:** pedido do usuário, fora do `ROADMAP.md` original — barra de busca acima da tabela de Cadastro de Colaboradores, ao lado do botão "+ Adicionar colaborador". Filtra por nome, código, CPF, e-mail ou usuário de acesso, sem diferenciar maiúscula/minúscula nem acento (`normalizarBusca`, `.normalize("NFD")` + remove diacríticos + `toLowerCase`). Filtro só no cliente, sobre a lista já carregada (sem chamada nova à API) — igual já acontece com o resto da tela. Visível pra qualquer perfil (Admin, Gerente, Coordenador), não só quem tem os botões de ação.
  - `listaFiltrada` (derivada de `lista` + `busca`) substitui `lista` só no que é renderizado (mapa da tabela, contagem de vazio); `lista` (sem filtro) continua sendo usada internamente onde precisa do registro completo (ex.: preservar `desligado` ao editar).
  - Mensagem de estado vazio muda pra `Nenhum colaborador encontrado para "<busca>".` quando a busca não acha ninguém, em vez da mensagem padrão de "nenhum colaborador cadastrado".
- **Motivo:** pedido do usuário — facilitar achar um colaborador específico em filiais com listas longas, sem precisar rolar a tabela inteira.
- **Impacto:** nenhuma mudança de contrato com a API. Testes: `CadastroColaboradores.test.tsx` — 4 casos novos (filtra por nome sem diferenciar maiúscula/acento; filtra por CPF/usuário de acesso; mostra a mensagem de vazio específica da busca sem afetar a lista completa depois de limpar o campo; Gerente não-admin também vê e usa a busca) — 23 testes no total, antes 19. Suíte completa: 286 testes, 286 passaram. `npx tsc -b --noEmit`, `npx oxlint` e `npm run build` sem erros. Verificado ao vivo (Playwright, servidor real, Admin, "Todas as filiais"): busca por "carlos" filtra pra só o colaborador certo; busca sem resultado mostra a mensagem correta com o termo buscado entre aspas; nenhum erro de console.

### 2026-08-19 — Colaboradores: perfil "Padrão" (sem acesso ao sistema)

- **O que foi modificado:** pedido do usuário, fora do `ROADMAP.md` original — 5ª opção de Perfil no Cadastro de Colaboradores, **Padrão**, pra colaboradores que precisam existir no cadastro (e nas demais tabelas que listam colaboradores) sem ter acesso nenhum ao sistema. `Papel` (`src/types/auth.ts`) ganhou o valor `"padrao"`; `NAV_POR_PAPEL.padrao` (`src/utils/constantes.ts`) é um array vazio — a guarda de rota do Shell (`F2.SHELL-04`, já existente) não libera nenhuma tela pra esse perfil, o mesmo mecanismo que já restringe Vendedor a Início/Consulta. `PAPEIS_COLABORADOR` ganhou `"padrao"` no fim da lista (dropdown do formulário) e `ROTULOS_PAPEL.padrao = "Padrão"` (rótulo da coluna Perfil da tabela e do próprio dropdown).
- **Motivo:** pedido do usuário — precisa cadastrar colaboradores que aparecem nas tabelas (Cadastro de Colaboradores e as demais telas que listam colaboradores, que não filtram por perfil) mas nunca fazem login de fato no sistema.
- **Impacto:** nenhuma mudança pros 4 perfis existentes (Vendedor/Coordenador/Gerente/Administrador) — só uma opção nova. Como nenhuma tela filtra colaboradores por perfil ao montar suas tabelas (Premiação, Comissão, Descontos, Consolidado PEV), um colaborador "Padrão" aparece nelas normalmente, sem código adicional. Testes: `CadastroColaboradores.test.tsx` — o teste que conferia as 4 opções do dropdown de Perfil foi atualizado pra 5, incluindo "Padrão". Suíte completa: 286 testes, 286 passaram. `npx tsc -b --noEmit`, `npx oxlint` e `npm run build` sem erros. **Não verificado ao vivo nesta rodada** — o backend real (`192.168.7.65:8000`) estava inacessível no momento da checagem (`ERR_CONNECTION_TIMED_OUT`), questão externa ao app; recomenda-se confirmar visualmente (dropdown mostrando "Padrão") numa próxima sessão.

### 2026-08-19 — Barra de pesquisa em Descontos e nas duas telas de colaborador de Plano de Saúde

- **O que foi modificado:** pedido do usuário, fora do `ROADMAP.md` original, estendendo a barra de busca que já existia em Cadastro de Colaboradores (entrada de 2026-08-18) pras telas de Descontos e Plano de Saúde:
  - `Descontos.tsx` — campo "Buscar colaborador" (nome ou código) ao lado dos botões de ação (Bloquear/Totais por tipo/Exportar), mesmo layout de `.acoes-tabela` com `space-between` já usado em Colaboradores. Filtra `colaboradores` antes de montar as linhas da tabela.
  - `LancamentoPlanoSaude.tsx` (sub-aba "Lançamento" de Plano de Saúde) — campo "Buscar titular/dependente" (nome ou código) ao lado do bloqueio/exportação. Filtra só o que é renderizado (`pessoasFiltradas`) — os totais do rodapé (Total ativos/desligados/geral) continuam somando todo mundo, buscado ou não, porque representam o que de fato será salvo.
  - `CadastroTitulares.tsx` (sub-aba "Titulares e Dependentes") — campo "Buscar titular/dependente" (nome ou CPF) acima da tabela. Como a tabela agrupa cada titular com a família dele, a busca mostra o titular inteiro (com todos os dependentes) se o titular **ou qualquer dependente dele** bater com o termo — buscar por um dependente também acha o grupo, sem esconder o titular que dá contexto de quem é a família.
  - `"Período do Plano"` (sub-aba de Plano de Saúde, Admin) ficou de fora — não é uma tabela de colaboradores, é uma lista de períodos de valor, não faz sentido buscar por nome/código ali.
  - Extraído `normalizarBusca` (usado pelas 4 telas agora) de `CadastroColaboradores.tsx` pra `src/utils/texto.ts`, com teste próprio (`texto.test.ts`) — evita duplicar a mesma função em 4 lugares.
- **Motivo:** pedido do usuário — mesma necessidade já resolvida em Colaboradores (filiais com listas longas), agora nas outras telas que também listam colaboradores por linha.
- **Impacto:** nenhuma mudança de contrato com a API — filtro só no cliente, sobre dados já carregados. Testes novos: `Descontos.test.tsx` (1 caso — filtra por nome, mensagem de vazio específica), `LancamentoPlanoSaude.test.tsx` (1 caso — filtra linhas sem alterar o Total ativos), `CadastroTitulares.test.tsx` (1 caso — filtra por titular e por dependente, família permanece visível). Suíte completa: 292 testes, 292 passaram (286 + 3 novos de busca + 3 de `texto.test.ts`). `npx tsc -b --noEmit`, `npx oxlint` e `npm run build` sem erros. Verificado ao vivo (Playwright, viewport 1280×900): como o backend real (`192.168.7.65:8000`) seguia inacessível (mesma questão externa já registrada em entradas anteriores), a verificação usou o adapter mock (troca local e temporária em `adapters/index.ts`, revertida logo em seguida — `git diff` confirmou nenhuma mudança residual). Os três campos de busca renderizam corretamente, alinhados com os botões de ação de cada tela; busca por "carlos" em Descontos filtra a lista visivelmente. Nenhum erro de console.

### 2026-08-19 — Ícone de ajuda "?" ao lado das buscas de Descontos e Lançamento (Plano de Saúde)

- **O que foi modificado:** pedido do usuário, complementando a entrada anterior (mesmo dia) — ícone "?" ao lado do rótulo "Buscar colaborador"/"Buscar titular/dependente" em `Descontos.tsx` e `LancamentoPlanoSaude.tsx`, que abre um painel de texto avisando que a busca só filtra o que é exibido na tela, sem afetar os totalizadores nem a exportação para Excel. Componente novo e genérico, `AjudaPopover` (`src/components/ui/AjudaPopover.tsx` + `.css`), reaproveitando o mesmo esquema de posicionamento/portal já corrigido no `MenuAcoes` mais cedo hoje (`right` em vez de `left`+`transform`, mais o `useLayoutEffect` que evita cortar embaixo/na lateral da janela) — não foi extraído um hook compartilhado entre os dois componentes por ora, pra não mexer de novo no `MenuAcoes` logo depois do conserto.
  - `CadastroColaboradores.tsx` e `CadastroTitulares.tsx` ficaram de fora — nenhuma das duas tem totalizador nem exportação (só cadastro/adesão), então o aviso não se aplica.
- **Motivo:** pedido do usuário — deixar explícito que a busca é só uma ajuda de navegação/preenchimento, não um filtro que muda o que é calculado ou exportado, evitando que alguém pense que só os colaboradores buscados entram no total ou no Excel.
- **Impacto:** nenhuma mudança de comportamento das buscas em si, só um ícone informativo a mais. Testes novos: `Descontos.test.tsx` e `LancamentoPlanoSaude.test.tsx` — 1 caso cada, confirmando que o painel abre ao clicar no botão "Ajuda" e contém o aviso esperado. Suíte completa: 294 testes, 294 passaram. `npx tsc -b --noEmit`, `npx oxlint` e `npm run build` sem erros. Verificado ao vivo (Playwright, viewport 1280×900, adapter mock pela mesma indisponibilidade do backend real já registrada, revertido em seguida sem alteração residual): os dois painéis abrem com o texto esperado, posicionados certinho abaixo do ícone mesmo com ele perto da borda esquerda da tela (a mesma lógica de não cortar na lateral, herdada do `MenuAcoes`, funcionou aqui também). Nenhum erro de console.

### 2026-08-20 — Paginação (10/50/100 linhas, avançar/voltar, números de página clicáveis) em todas as telas com tabela, exceto Início e Consulta

- **O que foi modificado:** pedido do usuário, fora do `ROADMAP.md` original — paginação adicionada em Colaboradores, Descontos, Comissão, Consolidado PEV, Premiação, e Titulares/Lançamento de Plano de Saúde (Período do Plano ficou de fora, mesmo critério já usado pra excluir da barra de busca — não é uma listagem de colaboradores). Início e Consulta ficaram de fora a pedido explícito do usuário. Três decisões confirmadas com o usuário antes de implementar (`AskUserQuestion`): tamanho de página padrão 10; Plano de Saúde paginado só nas 2 sub-abas de colaborador; em tabelas com agrupamento (colaborador com vários lançamentos, titular com dependentes), a página conta por **entidade principal**, não por `<tr>` renderizado — um titular e seus dependentes nunca ficam separados entre páginas.
  - Dois blocos novos e genéricos, reaproveitados nas 7 telas: `usePaginacao<T>` (`src/utils/usePaginacao.ts`) — recebe o array já filtrado/ordenado, devolve a fatia da página atual e os controles; volta pra página 1 sozinho quando a quantidade de itens muda (nova busca, troca de filial/mês, recarregamento). `Paginacao` (`src/components/ui/Paginacao.tsx` + `.css`) — seletor de linhas por página (10/50/100), texto "X–Y de Z", botões ‹/› e números de página clicáveis com janela de no máximo ~5 números + reticências pras extremidades, pra não virar uma parede de botões em listas grandes.
  - Nas telas com agrupamento, a "unidade" passada pro `usePaginacao` não é a lista de `<tr>`: `Descontos.tsx` pagina `colaboradoresFiltrados` (cada colaborador leva os lançamentos dele); `CadastroTitulares.tsx` pagina `titularesFiltrados` (cada titular leva os dependentes dele); `LancamentoPlanoSaude.tsx` precisou de uma função nova, `agruparPorTitular` (exportada do próprio arquivo, com teste dedicado), já que `pessoas` ali é uma lista plana de titulares+dependentes — agrupa por `titularId` antes de paginar, e achata o grupo da página de volta pra lista plana só na hora de renderizar.
  - Os totais/rodapés (Total geral, Total por tipo, Total ativos/desligados/geral) continuam somando **todos** os itens filtrados, nunca só os da página atual — mesma regra já usada pra busca não afetar totalizadores (ver entrada de 2026-08-19 sobre o ícone de ajuda).
- **Motivo:** pedido do usuário — tabelas grandes ficavam pesadas de rolar; paginação facilita navegação sem mudar nenhum cálculo.
- **Impacto:** nenhuma mudança de contrato com a API — paginação é só de exibição, sobre dados já carregados. Testes novos: `usePaginacao.test.ts` (5 casos — fatiamento, troca de página, troca de tamanho volta pra página 1, lista encolhe e a página se ajusta, nunca menos que 1 página mesmo vazia), `Paginacao.test.tsx` (6 casos — não renderiza sem itens, texto de faixa, desabilita ‹/› nos extremos, clique chama os callbacks certos, reticências em listas grandes), mais um caso por tela (`agruparPorTitular` com 5 casos dedicados no `LancamentoPlanoSaude.test.tsx`; um teste de integração em cada uma das 7 telas confirmando a contagem certa e, nas telas com totalizador, que ele não muda com a paginação). Suíte completa: 320 testes, 320 passaram (rodada com `--testTimeout=20000`, já que o arquivo `CadastroColaboradores.test.tsx` — o mais pesado, com digitação de formulário inteiro — ficou mais sensível à contenção de recursos da suíte completa depois do trabalho extra de render que a paginação adiciona; roda 100% limpo isolado ou com um timeout realista, sem indício de travamento real). `npx tsc -b --noEmit` sem erros. `npx oxlint`: 1 aviso novo, mesma categoria já tolerada em `SessaoContext.tsx` (export de função não-componente — `agruparPorTitular` — junto de um componente React no mesmo arquivo, atrapalha o Fast Refresh mas não é um erro). `npm run build` sem erros. Verificado ao vivo (Playwright, viewport 1280×900, adapter mock pela mesma indisponibilidade do backend real já registrada, revertido em seguida sem alteração residual): as 7 telas mostram a barra de paginação corretamente posicionada e estilizada abaixo da tabela, com a contagem certa; trocar "Linhas por página" funciona. Nenhum erro de console.

## Correções em funcionalidades prontas

### 2026-08-10 — `consolidadoPevServiceMock` (adapter mock, F1)

- **Erro/incompatibilidade encontrada:** o protótipo usa `vendedoresParaTela('premiacoes')` para listar quem aparece no Consolidado PEV — ou seja, só colaboradores com `telas.premiacoes = true`, o mesmo filtro da Planilha de Premiação (faz sentido: o PEV vem de lá). O `consolidadoPevServiceMock` de F1 listava todos os colaboradores da filial, sem checar `telas.premiacoes`.
- **O que foi alterado para corrigir:** `listarConsolidadoPev` (`src/adapters/mock/consolidadoPevService.mock.ts`) passou a filtrar também por `c.telas.premiacoes`.

### 2026-08-10 — `premiacaoServiceMock` (adapter mock, F1)

- **Erro/incompatibilidade encontrada:** ao construir a tela de Planilha de Premiação (F3.PREM), o protótipo mostrou que, com o Admin em "Todas as filiais", a planilha lista **todos** os colaboradores de todas as filiais e cada lançamento salvo grava a filial real do colaborador (`v.filial`), não uma filial única passada por parâmetro. O `premiacaoServiceMock` de F1 só filtrava/gravava por igualdade exata de filial, sem tratar `FILIAL_TODAS` — não sustentava esse caso.
- **O que foi alterado para corrigir:** `listarPremiacoes`/`salvarPremiacoes` (`src/adapters/mock/premiacaoService.mock.ts`) passaram a tratar `FILIAL_TODAS` como "sem filtro de filial" na leitura, e a gravar cada registro com a filial do próprio colaborador (`vendedor.filial`) em vez da filial recebida por parâmetro — mesmo padrão que `colaboradoresServiceMock` já usava desde F1.

### 2026-08-10 — `gerarIntervaloMeses` (utils, F1)

- **Erro/incompatibilidade encontrada:** ao construir o Consolidado PEV (F3.PEV), o protótipo mostrou que `gerarIntervaloMeses(de, ate)` gera os meses **cronologicamente entre `de` e `ate`, sem depender do ciclo Dez-Nov** — o ciclo (`anoCiclo`) é só uma sugestão inicial para preencher os filtros, não uma restrição. A versão de F1 exigia um `anoCiclo` e recortava dentro de um único ciclo de 12 meses, então um intervalo que cruzasse dois ciclos (ou anos distantes) retornava vazio incorretamente.
- **O que foi alterado para corrigir:** `gerarIntervaloMeses` (`src/utils/periodo.ts`) mudou de assinatura — `gerarIntervaloMeses(de, ate)`, sem `anoCiclo` — e passou a caminhar mês a mês cronologicamente entre as duas datas, replicando a função do protótipo. Testes de `src/utils/periodo.test.ts` atualizados de acordo.

### 2026-08-10 — `ConsultaService`/`consultaServiceMock` (F1)

- **Erro/incompatibilidade encontrada:** ao construir a tela de Consulta por Período (F3.CONS), o protótipo mostrou que a base de dados é `premiacoesDaFilial()` (filtrada pela filial da sessão, todas quando Admin em "Todas as filiais") e que cada linha do cartão precisa da filial do lançamento para mostrar a coluna "Filial" quando Admin vê todas. A interface `ConsultaService.listarConsulta` de F1 não recebia filial nenhuma (misturava lançamentos de todas as filiais sempre) e `CartaoMesConsulta` não tinha o campo `filial` por linha.
- **O que foi alterado para corrigir:** `listarConsulta` (`src/services/consultaService.ts`) ganhou um parâmetro `filial` (primeiro argumento); `CartaoMesConsulta.linhas` ganhou o campo `filial`; `consultaServiceMock` (`src/adapters/mock/consultaService.mock.ts`) passou a filtrar por filial (ou não filtrar quando `FILIAL_TODAS`) e a preencher `filial` a partir do próprio lançamento de premiação.
- **Bug real encontrado no mesmo arquivo:** o filtro de período usava `p.mesReferencia >= filtro.de && p.mesReferencia <= filtro.ate` direto — com `de`/`ate` vazios (filtro "ver todos os meses", o padrão da tela), a comparação `mesReferencia <= ""` é **sempre falsa** para qualquer mês real, então a tela sempre aparecia vazia por padrão. Só apareceu ao escrever os testes de F3.CONS com dados reais (F1 nunca testou o caso de filtro vazio). Corrigido para `(!filtro.de || ...) && (!filtro.ate || ...)`, tratando string vazia como "sem limite".

### 2026-08-10 — Cargo do seed não batia com as opções do formulário (`seed.ts`, F1)

- **Erro/incompatibilidade encontrada:** 4 dos 6 colaboradores seed tinham `cargo: "Vendedor"`, um valor que não existe em `CARGOS_COLABORADOR` (as opções do `<select>` de Função no cadastro). Isso fazia o campo Função exibir silenciosamente a primeira opção da lista em vez do cargo real, com risco de sobrescrever o cargo verdadeiro ao salvar sem querer, caso o usuário não tocasse nesse campo. Encontrado na verificação visual da modal de cadastro (F2.CAD) com Playwright, não pelos testes unitários (que não comparam contra a lista real de opções).
- **O que foi alterado para corrigir:** `src/adapters/mock/seed.ts` — `cargo: "Vendedor"` trocado por `cargo: "Consultor de Vendas Interno"` (um valor válido de `CARGOS_COLABORADOR`) nos 4 colaboradores afetados.

### 2026-08-11 — `comissaoServiceMock` (adapter mock, F1)

- **Erro/incompatibilidade encontrada:** ao construir a tela de Comissão (F4.COM), o mesmo padrão de bug já visto em F3 apareceu de novo: `buscar()` filtrava por igualdade exata de filial (`c.filial === filial`), então o Admin em "Todas as filiais" via a tela sempre vazia; e `salvarComissao` gravava a `filial` recebida por parâmetro em vez da filial real do colaborador, o que salvaria registros com `filial: "TODAS"` (um valor inválido) se o Admin estivesse vendo todas as filiais ao editar.
- **O que foi alterado para corrigir:** `buscar` (`src/adapters/mock/comissaoService.mock.ts`) passou a tratar `FILIAL_TODAS` como "sem filtro de filial"; `salvarComissao` passou a gravar `colaborador?.filial ?? existente?.filial ?? filial` em vez da filial recebida por parâmetro — mesmo padrão já usado em `premiacaoServiceMock` e `colaboradoresServiceMock`.

### 2026-08-11 — Caixas de entrada da tela de Descontos sem o estilo/tamanho da planilha (`Descontos.tsx`, F4)

- **Erro/incompatibilidade encontrada:** o protótipo usa `class="tabela tabela-planilha"` na tabela de Descontos (mesma classe da Planilha de Premiação e da Comissão), mas `Descontos.tsx` foi implementada com `<Table>` sem a prop `planilha`. Sem a classe `tabela-planilha`, os campos de Tipo/Valor/Observações ficavam sem o `width: 100%`/`min-width` que a limita ao tamanho da coluna — o `<select>` de Tipo, em especial, se auto-dimensionava pela opção mais longa ("Desconto autorizado (descrever em observações)"), alargando a tabela além da largura da tela e escondendo os botões "+ Adicionar"/"Remover" atrás de rolagem horizontal. Confirmado inspecionando a página ao vivo com Playwright: a tabela ficava 152px mais larga que o contêiner antes da correção.
- **O que foi alterado para corrigir:** `src/views/descontos/Descontos.tsx` — `<Table>` trocado por `<Table planilha>`, alinhando com o protótipo e com o mesmo padrão visual já usado em Comissão/Premiação.

### 2026-08-11 — Cadastro de Colaboradores não exigia e-mail (`CadastroColaboradores.tsx`, F8.CAD)

- **Erro/incompatibilidade encontrada:** `POST /api/usuarios` exige `email` (`Claude/API.md`: "obrigatório, único"), mas o campo E-mail do formulário nunca foi `required` — comportamento herdado de quando só o mock existia (que não valida e-mail). Confirmado ao vivo contra a API real, testando o campo de Perfil: cadastrar um colaborador sem e-mail voltou `400 {"mensagem":"erro ao cadastrar The email field is required."}` depois de ~18s (a resposta de erro do backend para esse endpoint está bem mais lenta que as demais chamadas testadas até aqui), com o toast de erro correto, mas a modal ficando aberta sem indicação prévia do motivo.
- **O que foi alterado para corrigir:** `CadastroColaboradores.tsx` — campo E-mail ganhou `required`; a validação em `tratarSubmit` passou a exigir e-mail preenchido junto com código/nome/CPF, evitando a viagem de ida e volta até a API para descobrir o problema.

### 2026-08-11 — Editar Admin/Gerente/Coordenador travava no campo Código (`CadastroColaboradores.tsx`, F8.CAD)

- **Erro/incompatibilidade encontrada:** desde que o filtro de perfil foi removido (evento "Campo de Perfil no Cadastro de Colaboradores"), a tela passou a listar/editar Admin/Gerente/Coordenador — mas esses colaboradores vêm com `"codigo": null` da API real (só vendedores têm código tipo `V001`). O campo Código do formulário era `required`, então abrir "Editar" num desses colaboradores pré-preenchia o campo vazio e o navegador bloqueava o `submit` silenciosamente com a validação nativa (nenhum toast, nenhuma chamada à API) — encontrado testando o efeito de carregamento ao vivo: o botão "Salvar alterações" nunca ficava com `aria-busy`. Isso sempre foi uma inconsistência com a API (`Claude/API.md`: `"codigo": "string, opcional, único"`), só nunca tinha aparecido porque até então a tela só mostrava vendedores (que sempre têm código).
- **O que foi alterado para corrigir:** `CadastroColaboradores.tsx` — `required` removido do campo Código; `tratarSubmit` não exige mais código (só nome, CPF e e-mail), alinhando com o que a API sempre aceitou.

### 2026-08-12 — Admin não entrava vendo "Todas as filiais" ao logar pela API real (`authServiceHttp`, F7-01)

- **Erro/incompatibilidade encontrada:** o documento técnico (Seção 1) especifica que o Administrador sempre é vinculado a "Todas (`FILIAL_TODAS`), pode restringir a uma no cabeçalho" — comportamento que `authServiceMock` já seguia desde F1/F2. `authServiceHttp` (`POST /api/valida-usuario`) usava `resposta.filial` (a filial vinculada ao próprio usuário admin no banco, ex.: `"100"`) direto como `filialAtiva`, então logar como Admin pela API real caía numa filial específica em vez de "Todas as filiais" — só percebido ao ler o documento técnico novamente.
- **O que foi alterado para corrigir:** `src/adapters/http/authService.http.ts` — `filialAtiva` passou a ser `FILIAL_TODAS` sempre que `resposta.role === "admin"`, ignorando `resposta.filial` nesse caso (mantida para os demais perfis). Teste novo em `authService.http.test.ts` cobrindo o caso.

### 2026-08-18 — Checkbox de adesão do dependente ignorava o valor real do banco (`planoSaudeServiceHttp`, F5.PS-CAD-06)

- **Erro/incompatibilidade encontrada:** ao ligar `salvarAdesaoDependente` no `PUT /api/dependentes/{id}` (entrada anterior, mesmo dia), a **leitura** ficou pra trás: `RespostaDependente`/`paraDependente` (`src/adapters/http/planoSaudeService.http.ts`) nunca mapeavam os campos `"plano saude"`/`"plano odontologico"` que `GET /api/dependentes` já devolve (`Claude/API (8).md`) — `listarDependentes` sempre voltava com `adesaoSaude`/`adesaoOdontologico` como `undefined`, que a convenção do app trata como "aderido" por padrão. Resultado: o checkbox do dependente sempre aparecia marcado ao carregar a tela, não importa o que estivesse salvo no banco — reportado pelo usuário.
- **O que foi alterado para corrigir:** `RespostaDependente` ganhou os dois campos; `paraDependente` passou a mapear `adesaoSaude`/`adesaoOdontologico` a partir deles, igual já era feito pro titular (`Colaborador`) há mais tempo.
- **Impacto:** nenhuma mudança de contrato. Testes: `planoSaudeService.http.test.ts` — os dois casos de `listarDependentes`/`salvarDependente` que exercitam `paraDependente` foram atualizados pra incluir `"plano saude"`/`"plano odontologico"` na resposta simulada e conferir que viram `adesaoSaude`/`adesaoOdontologico` no resultado (antes não cobriam esses campos, por isso o bug passou despercebido). Suíte completa: 253 testes, 253 passaram. `npx tsc -b --noEmit`, `npx oxlint` e `npm run build` sem erros.

### 2026-08-11 — `premiacaoServiceHttp` quebrado pela nova resposta agrupada por mês da API (F8.PREM)

- **Erro/incompatibilidade encontrada:** a versão anterior de `GET /api/premiacoes` (`Claude/API (2).md`) devolvia um array plano `"dados"` com o total do período inteiro pedido. A versão atual (`Claude/API (3).md`), confirmada ao vivo contra o backend real, passou a agrupar a resposta por mês em `"meses": [{ "mes de referencia", dados: [...], totais: {...} }]`, ordenado do mês mais recente para o mais antigo. `premiacaoServiceHttp.listarPremiacoes` (que lia `resposta.dados[0]` direto) teria passado a devolver sempre valores zerados/`undefined` para todo mundo, já que esse campo não existe mais na resposta.
- **O que foi alterado para corrigir:** `src/adapters/http/premiacaoService.http.ts` reescrito para ler `resposta.meses[0]?.dados[0]` em vez de `resposta.dados[0]` (como cada requisição já é filtrada por `data_inicio`/`data_fim` de um único mês, `meses` tem no máximo uma entrada). A leitura da resposta foi extraída para o módulo compartilhado `src/adapters/http/respostaPremiacoesAgrupadas.ts` (ver evento "Consulta por Período consumindo a API real" acima), usado também por `consultaServiceHttp`. `src/adapters/http/premiacaoService.http.test.ts` foi atualizado para mockar o novo formato agrupado.

### 2026-08-18 — Lançamento (Odontológico) mostrava o valor de um período antigo já encerrado em vez do vigente (`encontrarPeriodoPlano`)

- **Erro/incompatibilidade encontrada:** reportado pelo usuário — depois de cadastrar um novo período vigente de Odontológico pra Titular/Dependente na filial 100 (valores 100/110), a aba Lançamento continuava mostrando o valor antigo do seed (R$ 13,56), como se estivesse "fixo" em vez de vir do banco. Investigando ao vivo contra `GET /api/valores-plano-saude?filial=100&tipo_plano=odontologico`: a API **não garante nenhuma ordem específica** na resposta — o período seed antigo (`dataCriacao` `2000-01-01`, já encerrado com `dataValidade` "2026-08-18", ou seja, encerrado só hoje, no meio do mês corrente) apareceu **depois** do período novo e realmente vigente. `encontrarPeriodoPlano` (`src/services/planoSaudeService.ts`) decidia empate por posição no array (percorria de trás pra frente, "o mais recentemente cadastrado" = o que aparece por último) — presumia que a API devolvia em ordem de cadastro, o que não é verdade. Como o seed encerrado ainda cobria parte do mês corrente (`dataValidade >= início do mês`) e aparecia depois do vigente na resposta, a busca por posição pegava o registro errado.
- **O que foi alterado para corrigir:** `encontrarPeriodoPlano` reescrita pra não depender de posição — filtra todos os candidatos que batem no mês e, entre eles, sempre prefere o que está `ativo` (só pode existir um por filial+tipo de plano+tipo de pessoa); sem nenhum vigente batendo, o de `dataCriacao` mais recente prevalece (comparação de datas de verdade, não posição no array).
- **Impacto:** nenhuma mudança de contrato — a função continua com a mesma assinatura, só o critério de desempate mudou. Testes: `src/services/planoSaudeService.test.ts` (novo arquivo — a função nunca tinha teste dedicado, só cobertura indireta via `LancamentoPlanoSaude.test.tsx`) com 4 casos, incluindo um que reproduz exatamente o cenário real do bug (período antigo encerrado aparecendo depois do vigente no array). Suíte completa: 262 testes, 262 passaram. Verificado ao vivo (Playwright, servidor real, filial 100, só leitura): Odontológico agora mostra R$ 100,00 (Titular)/R$ 110,00 (Dependente), batendo com o período realmente vigente confirmado via `curl`; nenhum erro de console.

### 2026-08-18 — Período do Plano: Data de Encerramento futura encerrava a vigência antes da hora

- **Erro/incompatibilidade encontrada:** reportado pelo usuário — o modal "+ Novo período" deixava preencher uma Data de Encerramento no futuro (ex.: daqui a duas semanas) e, como `dataFim` sempre nasce o período `ativo: false` (tanto no mock quanto na API real, que documenta esse comportamento em `Claude/API (8).md`: "cadastrando data inicio e data fim juntas, o período já nasce encerrado"), o período ficava marcado como encerrado **imediatamente**, mesmo a data de encerramento ainda não tendo chegado — confirmado ao vivo contra o backend real (`GET /api/valores-plano-saude?tipo_plano=odontologico`): dois períodos de teste da filial 100 ficaram com `"data validade": "2026-08-30"` (futuro) e `"ativo": false` já no cadastro, em 2026-08-18.
- **O que foi alterado para corrigir:** `CadastroPeriodoPlano.tsx` ganhou uma validação nova — se a Data de Encerramento informada for posterior a hoje, o cadastro é recusado com uma mensagem explicando que a Data de Encerramento só pode ser hoje ou no passado (pra criar direto um período já histórico); pra uma data futura, o Admin precisa deixar o campo em branco e usar "Encerrar vigência" quando o dia chegar. O campo de data também ganhou `max={hoje}` (o seletor do navegador já não deixa escolher uma data futura). Texto de ajuda da modal atualizado explicando a regra.
- **Impacto:** nenhuma mudança pra quem preenche Data de Encerramento com uma data passada ou de hoje (comportamento idêntico a antes). Testes: `CadastroPeriodoPlano.test.tsx` ganhou um caso novo (rejeita Data de Encerramento futura, modal continua aberta, nada é criado); o teste existente que criava um período já histórico foi ajustado pra usar uma data passada de verdade em vez de uma que só parecia passada quando a suíte foi escrita mas virou futura com o tempo (14 testes no total). Suíte completa: 262 testes, 262 passaram. `npx tsc -b --noEmit`, `npx oxlint` e `npm run build` sem erros. **Períodos de teste com essa inconsistência (filial 100, Odontológico Titular/Dependente, `dataValidade` "2026-08-30", já `ativo: false`) continuam no backend real** — não foram limpos por não ter autorização de escrita nesta sessão; recomenda-se ao usuário encerrar/corrigir manualmente ou autorizar uma limpeza numa próxima sessão.

### 2026-08-18 — Lançamento: rodapé com colunas "—" em vez de calculadas, e Total desligados sem quebra por coluna (F5.PS-LAN-05b)

- **Erro/incompatibilidade encontrada:** pedido do usuário pra ajustar a linha de Total desligados (recém-criada, ver "Alterações não planejadas" acima) — as colunas Titular/Dependente/Adicional/Coopart. das linhas Total desligados e Total geral mostravam só um traço "—" (não calculado), e Total desligados só tinha um campo único (a coluna Total) em vez de campos preenchíveis por coluna.
- **O que foi alterado para corrigir:** `LancamentoPlanoSaude.tsx` — a linha Total desligados ganhou 4 campos editáveis (Titular/Dependente/Adicional/Coopart., os dois últimos só em Saúde), cuja soma vira a coluna Total dessa linha (`totalDesligadosValor`, o que de fato é enviado a `salvarTotalDesligadosPlanoSaude`). A linha Total geral passou a somar, coluna a coluna, o Total ativos + a quebra digitada em Total desligados. Como a API só grava um `valor` único (sem quebra por coluna, `Claude/API (8).md`), a quebra por coluna não sobrevive a um recarregamento — só o total volta (`totalDesligadosValor`, carregado de `GET /api/lancamentos`); os 4 campos voltam zerados até serem preenchidos de novo. Decisão de trade-off confirmada com o usuário antes de implementar (a alternativa era manter um campo único). Texto de ajuda novo, abaixo da tabela, explicando essa limitação.
- **Motivo:** pedido do usuário — "todos os campos dos totalizadores precisam ser calculados e nos desligados podem ser preenchidos".
- **Impacto:** nenhuma mudança de contrato com a API (continua enviando um `valor` único). Testes: `LancamentoPlanoSaude.test.tsx` — teste de rodapé reescrito pra preencher os 4 campos de desligados e conferir que a coluna Total da linha soma certo, e que a linha Total geral soma coluna a coluna; teste de "Todas as filiais" atualizado pra conferir os 4 campos desabilitados (11 testes no total, mesma contagem). Suíte completa: 262 testes, 262 passaram. `npx tsc -b --noEmit`, `npx oxlint` e `npm run build` sem erros. Verificado ao vivo (Playwright, servidor real, filial 100, Odontológico, só leitura): Total ativos/Total geral mostram valores calculados (nada de "—"); Total desligados mostra os 2 campos (Titular/Dependente, sem Adicional/Coopart. por ser Odontológico) vazios e editáveis, com "R$ 0,00" calculado na coluna Total; texto de ajuda visível. Nenhum erro de console.

### 2026-08-18 — Colaboradores: botão "+ Adicionar colaborador" esticado depois da barra de busca (F2.CAD-03)

- **Erro/incompatibilidade encontrada:** reportado pelo usuário logo depois da barra de busca (entrada anterior, mesmo dia) — o botão "+ Adicionar colaborador" ficou esticado verticalmente, do topo do rótulo "Buscar colaborador" até a base do campo, em vez de alinhado só com o campo de busca. Causa: `.acoes-tabela` é `display: flex` sem `align-items` definido (`stretch` por padrão) — como o campo de busca (rótulo + input) ficou mais alto que o botão sozinho, o botão esticava pra preencher a altura do irmão mais alto.
- **O que foi alterado para corrigir:** `CadastroColaboradores.tsx` — a `div.acoes-tabela` que envolve os dois ganhou `alignItems: "flex-end"` inline, alinhando o botão pela base (mesmo padrão já usado em `.grade-formulario`, `align-items: end`).
- **Impacto:** só visual — nenhuma mudança de comportamento. Não gerou teste novo (é puramente de CSS, sem asserção de layout na suíte). Suíte completa: 286 testes, 286 passaram (sem alteração). `npx tsc -b --noEmit`, `npx oxlint` e `npm run build` sem erros. Verificado ao vivo (Playwright, servidor real, Admin): botão alinhado com a base do campo de busca, mesma altura, sem esticar. Nenhum erro de console.

### 2026-08-18 — Colaboradores: altura do campo de busca diferente do botão "+ Adicionar colaborador" (F2.CAD-03)

- **Erro/incompatibilidade encontrada:** reportado pelo usuário logo depois do ajuste do alinhamento (entrada anterior, mesmo dia) — mesmo alinhados pela base, o campo de busca (50px) ficava ~3px mais alto que o botão (~46,8px). Causa: `<button>` não tem borda (reset global, `button { border: none; }`), enquanto `.campo input` tem `border: 1.5px solid var(--cor-divider)` — com `box-sizing: border-box` global, essa borda soma à altura total do input mas não à do botão.
- **O que foi alterado para corrigir:** `formularios.css` — nova regra escopada `.acoes-tabela .campo input` (não mexe no `.campo input` genérico usado no resto do app) fixando `height: 47px` e zerando `padding-top`/`padding-bottom`, igualando a altura renderizada à do `.botao` ao lado.
- **Impacto:** só visual — nenhuma mudança de comportamento. Não gerou teste novo (é puramente de CSS). Suíte completa: 286 testes, 286 passaram (sem alteração). `npx tsc -b --noEmit`, `npx oxlint` e `npm run build` sem erros. Verificado ao vivo (Playwright, servidor real, Admin, `boundingBox()`): diferença de altura caiu de ~3,2px para ~0,2px (input 47px, botão 46,8px), imperceptível visualmente. Nenhum erro de console.

### 2026-08-19 — Colaboradores: popup do menu "⋮" longe do ícone e cortado perto da borda da tela (F2.CAD-04)

- **Erro/incompatibilidade encontrada:** reportado pelo usuário — o popup do menu de ações (`MenuAcoes`) abria longe do ícone "⋮" em vez de colado nele, e ficava cortado tanto embaixo quanto na lateral quando a linha estava perto do fim da tela. Causa raiz do primeiro problema: o posicionamento horizontal usava `left: rect.right` + `transform: translateX(-100%)` inline pra alinhar a borda direita do menu com a do botão, mas a animação de entrada (`@keyframes surgir`, `global.css`, já usada em vários componentes) anima a propriedade `transform` (`translateY`) — como CSS só permite um valor de `transform` por vez, a animação sobrescrevia o `translateX(-100%)` assim que terminava (140ms), fazendo o menu "pular" pra direita do botão em vez de ficar alinhado por baixo dele. O segundo problema (corte embaixo/lateral) nunca teve tratamento nenhum: a posição era calculada uma única vez, sem checar se o menu cabia na janela.
- **O que foi alterado para corrigir:** `MenuAcoes.tsx` — posicionamento horizontal passou de `left` + `transform` pra `right: window.innerWidth - rect.right` (propriedade independente de `transform`, não conflita com a animação). Novo `useLayoutEffect` mede o menu já renderizado e, se estourar a borda inferior da janela, reabre pra cima do botão (`top = botão.top - altura do menu - 4`); se estourar a borda esquerda, afasta o menu dela (`right` recalculado a partir da largura real medida) — ambos com uma margem de 8px (`MARGEM_JANELA`).
- **Motivo:** pedido do usuário — o popup precisa abrir sempre colado no ícone que o abriu e nunca cortado, em qualquer posição da tabela.
- **Impacto:** nenhuma mudança de comportamento das ações do menu em si, só de posicionamento. Nenhum teste da suíte dependia da implementação anterior (posição não era asserida). Suíte completa: 286 testes, 286 passaram (sem alteração). `npx tsc -b --noEmit`, `npx oxlint` e `npm run build` sem erros. Verificado ao vivo (Playwright, viewport 1280×720): como o backend real (`192.168.7.65:8000`) seguia inacessível (CORS/timeout — mesma questão externa já registrada em entradas anteriores), a verificação usou o adapter mock (troca local e temporária em `adapters/index.ts`, revertida logo em seguida — `git diff` confirmou nenhuma mudança residual no arquivo). Linha do topo: menu abre 4px abaixo do botão com a borda direita exatamente alinhada à do botão (antes ficava deslocado pra direita). Última linha da tabela (que estouraria a borda inferior da janela): menu abre pra cima do botão, com a borda inferior calculada em `top = botão.top - altura do menu - 4`, permanecendo inteiramente dentro da janela. Nenhum erro de console.

### 2026-08-19 — Colaboradores: campo E-mail deixa de ser obrigatório no formulário (F2.CAD-04)

- **Erro/incompatibilidade encontrada:** reportado pelo usuário — o formulário exigia E-mail (`required` no `<input>` + checagem em `tratarSubmit`) para qualquer colaborador, mas o `ROADMAP.md` (F2.CAD-04) só lista código, nome, CPF e filial como campos exigidos pela validação — E-mail nunca esteve nessa lista. Ficou mais evidente com o perfil "Padrão" (entrada de 2026-08-19, mais cedo) — colaboradores sem acesso ao sistema, tipicamente sem e-mail corporativo, ficavam bloqueados de cadastrar por causa de um campo que a especificação nunca marcou como obrigatório.
- **O que foi alterado para corrigir:** `CadastroColaboradores.tsx` — removido o `required` do `<input id="colaborador-email">` e o `!formulario.email` da checagem em `tratarSubmit` (que agora só exige nome e CPF, junto com a checagem de filial já existente em separado). Mensagem de erro do toast ajustada de "Preencha ao menos nome, CPF e e-mail" para "Preencha ao menos nome e CPF".
- **Motivo:** pedido do usuário. **Decisão consciente registrada aqui:** a API real (`Claude/API (11).md`, `POST /api/usuarios`) continua exigindo `"email": "string, obrigatório, único"` no servidor — cadastrar sem e-mail contra o backend real vai ser recusado com um erro, mostrado no toast de erro do formulário (o formulário não trava mais o clique em "Cadastrar", mas o servidor ainda pode). Essa divergência entre validação do front e do back foi perguntada ao usuário antes de implementar (opção de tornar opcional só pro perfil Padrão vs. pra todos) — escolhida "opcional pra todos", ciente do risco.
- **Impacto:** nenhuma mudança pra quem já preenche e-mail normalmente. Testes: `CadastroColaboradores.test.tsx` — o teste que validava "nome, CPF e e-mail obrigatórios" foi renomeado e ajustado pra "nome e CPF" (e-mail branco não bloqueia mais o clique); caso novo cadastrando um colaborador sem e-mail contra o adapter mock (que não valida e-mail, então salva normalmente) — 24 testes no total, antes 23. Suíte completa: 295 testes, 295 passaram. `npx tsc -b --noEmit`, `npx oxlint` e `npm run build` sem erros. **Não verificado ao vivo contra o backend real nesta rodada** — o teste que prova o comportamento roda contra o adapter mock; contra a API real, o resultado esperado é um erro do servidor (não um bug), mas não foi confirmado ao vivo por falta de acesso ao backend nesta sessão (mesma indisponibilidade já registrada em entradas anteriores do dia).

### 2026-08-20 — Barra de busca derrubava a tela inteira (branco) ao pesquisar um colaborador sem e-mail

- **Erro/incompatibilidade encontrada:** reportado pelo usuário — a tela ficava toda branca ao digitar na barra de busca de Colaboradores. Causa raiz, consequência direta da entrada anterior (e-mail deixou de ser obrigatório, 2026-08-19): `colaboradoresService.http.ts` já tratava `codigo: string | null` vindo da API (`resposta.codigo ?? ""`), mas `email` estava tipado como `string` simples e mapeado sem tratamento (`email: resposta.email`). Um colaborador cadastrado sem e-mail volta da API real como `email: null` — e `normalizarBusca` (usada pelas 4 telas com busca: Colaboradores, Descontos, Lançamento e Titulares de Plano de Saúde) chamava `.normalize()` direto em cada campo buscado, inclusive e-mail. `null.normalize()` estoura, e como não existia nenhum *error boundary*, o React derruba a árvore inteira pra tela em branco — só acontecia ao digitar (não ao carregar a tela) porque o filtro só roda `normalizarBusca` campo a campo quando há termo de busca.
- **O que foi alterado para corrigir:** dois níveis, pra não depender só de lembrar de tratar cada campo novo no futuro — `colaboradoresService.http.ts`: `RespostaUsuario.email` tipado `string | null` e mapeado com `resposta.email ?? ""`, igual já era feito com `codigo`; `src/utils/texto.ts`: `normalizarBusca` passou a aceitar `string | null | undefined` e tratar valor ausente como `""`, em vez de estourar — proteção na função compartilhada em si, não só no ponto onde o bug apareceu, já que ela processa campos vindos direto da API em 4 telas diferentes.
- **Motivo:** pedido do usuário — busca não pode derrubar a tela, e novos campos opcionais vindos da API não deveriam poder repetir esse mesmo problema em outro lugar.
- **Impacto:** nenhuma mudança pra colaboradores com e-mail preenchido. Testes: `texto.test.ts` — caso novo confirmando que `null`/`undefined` viram `""` sem estourar; `colaboradoresService.http.test.ts` — o teste que já provava `codigo: null` virando `""` ganhou a mesma asserção pra `email: null`; `CadastroColaboradores.test.tsx` — o teste que cadastra um colaborador sem e-mail ganhou um passo a mais, buscando por esse colaborador logo em seguida e confirmando que a tela continua renderizada (regressão direta do bug). Suíte completa: 296 testes, 296 passaram. `npx tsc -b --noEmit`, `npx oxlint` e `npm run build` sem erros. **Não verificado ao vivo contra o backend real nesta rodada** (mesma indisponibilidade já registrada em entradas anteriores) — a regressão foi reproduzida e corrigida via o adapter mock, com o cenário exato (colaborador sem e-mail, e-mail vindo `null`) coberto tanto no teste do adapter HTTP quanto no teste de UI.

### 2026-08-20 — Investigação: Consulta e Premiação, uma chamada por colaborador — reconfirmado, sem mudança de código

- **O que foi investigado:** pedido do usuário — conferir se `GET /api/premiacoes` (usado tanto por `premiacaoServiceHttp.listarPremiacoes` quanto por `consultaServiceHttp.listarConsulta`, via o helper compartilhado `buscarPremiacoesAgrupadas` em `src/adapters/http/respostaPremiacoesAgrupadas.ts`) permite uma única chamada sem `?id=`, devolvendo todos os colaboradores de uma vez, em vez de uma chamada por colaborador.
- **Conclusão:** a API permite, sim (`id` é opcional em `Claude/API (13).md`) — mas a resposta agrupada por mês **continua sem trazer nenhum id de colaborador por linha**, só `"nome colaborador"` e `"filial"` (mesma limitação já registrada em 2026-08-10/2026-08-11, quando esse mesmo workaround de uma-chamada-por-colaborador foi desenhado pela primeira vez, ver entradas mais antigas deste arquivo e o comentário em `respostaPremiacoesAgrupadas.ts`). Uma única chamada exigiria casar cada linha de volta ao colaborador certo por nome (+filial), o que arrisca atribuir a premiação de uma pessoa a outra caso duas pessoas da mesma filial tenham o mesmo nome — inaceitável pra dado de premiação/pagamento. Perguntado ao usuário três opções (manter, trocar mesmo com o risco, ou só documentar) — escolhida **manter como está e só documentar**.
- **Impacto:** nenhuma mudança de código. Nenhum teste novo. Registrado aqui pra não precisar reinvestigar do zero numa sessão futura — a limitação (falta de `id colaborador` por linha) segue de pé na versão atual da API; se algum dia `GET /api/premiacoes` passar a devolver um id por linha, aí sim compensa revisitar e trocar pra uma chamada só.

### 2026-08-20 — `Claude/API (15).md` resolve a pendência acima: Premiação e Consulta passam a fazer 1 chamada só (não mais uma por colaborador), e a exportação CSV passa a vir pronta do backend

- **O que foi modificado:** pedido do usuário, revisitando a investigação da entrada anterior com o novo documento de API — `GET /api/premiacoes` agora traz `"id colaborador"`/`"codigo"` em cada linha (antes só `"nome colaborador"`), resolvendo o motivo que forçava uma requisição por colaborador. Antes de implementar, testado ao vivo contra o backend real (login com credenciais reais fornecidas pelo usuário) pra confirmar duas afirmações do usuário que se mostraram **incorretas** na prática, evitando um bug: (1) "o backend traz o colaborador mesmo sem lançamento no mês" — testado com 2 filiais reais onde um colaborador tem a tela Premiações mas nunca recebeu lançamento: a resposta veio `"meses": []`, completamente vazia, sem nenhuma linha zerada — **a Planilha continua precisando da lista de colaboradores** pra saber quem deve ganhar uma linha editável; (2) a exportação CSV do backend (`GET /api/premiacoes/exportar-csv`) tem colunas diferentes do CSV que o front montava (`"nome colaborador";pev;iconic;filtros;fornecedores;inadimplencia;total` — sem CPF, sem Observações, categorias separadas em vez de só "Valor Total") — confirmado com o usuário que a troca vale mesmo assim.
  - **`respostaPremiacoesAgrupadas.ts`**: `RespostaPremiacaoLinha` ganhou `"id colaborador"`/`codigo`; `buscarPremiacoesAgrupadas` teve o parâmetro `colaboradorId` tornado opcional — informado, filtra um colaborador só (usado no escopo de vendedor, por privacidade, ver abaixo); omitido, a API devolve a filial inteira numa chamada. Novo `buscarCsvPremiacoes(...)`, que busca `GET /api/premiacoes/exportar-csv`, decodifica o base64 e **reinterpreta os bytes como UTF-8** (`atob()` sozinho corromperia acento). Ambas as funções agora omitem `?filial=` quando o valor é `FILIAL_TODAS` (sentinela só do front — antes só era chamada com a filial de um colaborador específico, nunca "todas", então esse caso nunca tinha aparecido).
  - **`premiacaoServiceHttp.listarPremiacoes`**: continua buscando a lista de colaboradores (pro roster de quem tem a tela Premiações), mas troca as N chamadas por colaborador por **1 chamada só** pra filial inteira, casando cada linha de volta pelo `id colaborador` (antes seria arriscado por nome; agora é seguro). Colaborador sem linha na resposta continua ganhando valores zerados — mesmo comportamento de antes, só que calculado depois de 1 chamada em vez de N.
  - **`consultaServiceHttp.listarConsulta`**: como Consulta nunca precisou de linha zerada pra quem não tem lançamento (é só leitura do que já existe) e CPF nunca apareceu em tela (só era usado no CSV, que agora vem pronto do backend), a chamada a `colaboradoresServiceHttp.listarColaboradores` foi **eliminada por completo** — 1 chamada a `/api/premiacoes` e pronto. Quando `escopo` (perfil vendedor) é informado, essa chamada já vem filtrada por `?id=` do próprio vendedor — o navegador dele nunca recebe premiação de mais ninguém, mesma cautela de privacidade de antes.
  - **Exportação CSV (`PremiacaoService`/`ConsultaService`)**: `exportarPremiacoesCSV`/`exportarCSV` viraram métodos assíncronos das interfaces de serviço (antes era uma função síncrona em `premiacaoService.ts`, montando o CSV no cliente). `premiacaoServiceHttp`/`consultaServiceHttp` chamam `buscarCsvPremiacoes` e baixam o arquivo já pronto (`baixarCSVPronto`, novo em `utils/exportar.ts`); os adapters mock replicam as mesmas colunas do backend real (montadas localmente, já que o mock não tem servidor pra chamar), pra ficar representativo do que a tela realmente recebe. `Premiacao.tsx`/`ConsultaPeriodo.tsx` ganharam estado `exportando` e o botão de exportar agora mostra o spinner de carregamento (`carregando`) enquanto a chamada está em voo — antes era instantâneo (síncrono), agora depende de rede.
- **Motivo:** pedido do usuário — reduzir o número de chamadas nas telas de Consulta e Premiação, aproveitando que a API resolveu a limitação que forçava uma chamada por colaborador; e usar a exportação CSV pronta do backend em vez de montar no front.
- **Impacto:** Premiação/Consulta continuam mostrando exatamente os mesmos dados de antes (nenhuma mudança de comportamento visível na tela, só menos chamadas de rede). CSV exportado muda de colunas (perde CPF/Observações, ganha PEV/Iconic/Filtros/Fornecedores/Inadimplência separados) — mudança visível e esperada, confirmada com o usuário. Testes: `premiacaoService.http.test.ts`/`consultaService.http.test.ts` reescritos pra provar 1 chamada em vez de N, mais casos novos de exportação CSV (decodifica UTF-8 corretamente, erro quando não há lançamento pra exportar, `?id=` no escopo de vendedor, omite `?filial=` em "Todas as filiais"). Suíte completa: 326 testes, 326 passaram, sem flakiness nesta rodada. `npx tsc -b --noEmit` e `npx oxlint` sem erros novos. `npm run build` sem erros. **Verificado ao vivo contra o backend real** (`http://192.168.7.65:8080`, credenciais reais fornecidas pelo usuário, login master) — a essa altura o backend já estava acessível de verdade (mudou de porta pra `8080` desde a última tentativa da sessão, `.env` já atualizado): Planilha de Premiação carregou os 3 colaboradores reais com a tela habilitada, cada um com linha zerada editável (confirmando visualmente que a lista de colaboradores continua necessária); rede mostrou 1 única chamada a `/api/premiacoes` (não mais uma por colaborador); clique em "Exportar CSV da filial" gerou um download real com toast de sucesso; Consulta carregou sem nenhuma chamada a `/api/usuarios`. Nenhum erro de console na verificação final (um `500` transitório apareceu numa tentativa anterior com timing mais apertado, não reproduzido depois — mesma instabilidade do ambiente local/debug já documentada em `Claude/API (15).md`).

### 2026-08-20 — `Claude/API (16).md` resolve a última pendência: Premiação também elimina a chamada de colaboradores (perde a coluna CPF)

- **O que foi modificado:** pedido do usuário, revisitando a entrada anterior (mesmo dia) com o `Claude/API (16).md` — a API ganhou o **"roster do mês"**: quando `data_inicio`/`data_fim` caem no mesmo mês (exatamente como `premiacaoServiceHttp` já monta a chamada), `GET /api/premiacoes` agora devolve também os colaboradores com a tela Premiações que ainda não lançaram nada, já zerados — resolvendo o motivo exato que impedia eliminar a chamada de colaboradores na entrada anterior. Confirmado ao vivo antes de implementar (não só confiado no changelog): logado como master, `GET /api/premiacoes?filial=101&data_inicio=2026-08-01&data_fim=2026-08-31` e `?filial=402&...` — as duas devolveram os colaboradores habilitados (239, 240, 247) já zerados, sem nenhum lançamento real. A resposta continua sem CPF por linha, e diferente da Consulta (onde CPF só existia pro CSV, hoje gerado pelo backend), a Planilha de Premiação mostra CPF como **coluna visível na tela** — perguntado ao usuário se valia perder essa coluna pra eliminar a chamada; confirmado que sim.
  - `Premiacao` (`src/types/premiacao.ts`) ganhou o campo `codigo` (a API já devolvia, só não era capturado).
  - `premiacaoServiceHttp.listarPremiacoes` (`src/adapters/http/premiacaoService.http.ts`) não busca mais `colaboradoresServiceHttp.listarColaboradores` — monta o roster inteiro direto de `resposta.meses[0]?.dados`, sem precisar cruzar com nada. `somarCategoriasPremiacao` deixou de ser necessário aqui (a API sempre devolve `total`, mesmo zerado).
  - `premiacaoServiceMock` (`src/adapters/mock/premiacaoService.mock.ts`) ganhou `buscarComRoster`, que replica o mesmo comportamento (uma linha por habilitado, zerada quando não há lançamento salvo) — sem isso o mock ficaria com um contrato diferente do adapter real agora que `Premiacao.tsx` não faz mais o merge de roster sozinho.
  - `Premiacao.tsx`: o estado `colaboradores: Colaborador[]` (vinha de uma chamada separada) virou `roster: Premiacao[]` (vem direto de `listarPremiacoes`) — a tela não monta mais o "zera quem não tem lançamento" por conta própria, só usa o que a API já manda pronto. Coluna CPF removida da tabela (`colSpan` dos estados vazios ajustado de 10 pra 9 colunas fixas).
- **Motivo:** pedido do usuário — última chamada redundante que sobrava depois da otimização de Consulta, agora que a API também resolveu o motivo que a mantinha em Premiação.
- **Impacto:** Planilha de Premiação perde a coluna CPF (mudança visível, confirmada com o usuário) — Código/Colaborador/Filial continuam. Nenhuma mudança de comportamento de edição/salvamento/bloqueio. Testes: `premiacaoService.http.test.ts` reescrito (sem fixture de `/api/usuarios`, roster vem só da resposta de premiações); `premiacaoService.mock.test.ts` (arquivo pré-existente, não visto na entrada anterior) teve 2 dos 3 casos ajustados — a suposição de "só quem foi salvo aparece" não vale mais, já que o roster agora inclui todo habilitado, zerado ou não. Suíte completa: 326 testes, 326 passaram (rodada com `--testTimeout=20000`, mesma sensibilidade a contenção de recursos já registrada — 100% limpo isolado). `npx tsc -b --noEmit` e `npx oxlint` sem erros novos. `npm run build` sem erros. **Verificado ao vivo contra o backend real** (login master): Planilha carregou os 3 colaboradores reais corretamente, sem coluna CPF, com **zero chamadas a `/api/usuarios`** (confirmado via interceptação de rede) — só 2 chamadas a `/api/premiacoes` (StrictMode duplica em desenvolvimento). Nenhum erro de console.

### 2026-08-20 — Consulta: "roster do mês" (entrada anterior) vazava cartões fantasma de R$0,00 no filtro padrão (De=Até, mesmo mês)

- **Erro/incompatibilidade encontrada:** pedido do usuário pra revisar Consulta depois da mudança em Premiação — o "roster do mês" do `Claude/API (16).md` só dispara quando `data_inicio`/`data_fim` caem no mesmo mês, e o filtro padrão de Consulta (`de = ate = obterMesPassadoISO()`) é **exatamente** esse caso. Confirmado ao vivo antes de implementar: `GET /api/premiacoes?filial=101&data_inicio=2026-08-01&data_fim=2026-08-31` devolvia o colaborador 239 (tela Premiações habilitada, nunca lançou nada) com valores zerados; pedindo um intervalo maior que 1 mês (`data_fim=2026-09-01`, ou julho+agosto juntos), ele some — confirmando que é artefato só da consulta de um único mês, não dado real. Na prática: Consulta (tela de **histórico só do que foi de fato lançado**, diferente da Planilha de Premiação, que é a tela de **cadastro** e precisa mesmo do roster inteiro) passou a mostrar cartões de "R$ 0,00" pra colaboradores que nunca tiveram premiação nenhuma — inclusive um caso já visto ao vivo numa entrada anterior do mesmo dia (Julho de 2026, "3 lançamento(s)") que, ao reinvestigar, também era esse mesmo artefato, não dado real.
- **O que foi alterado para corrigir:** sem voltar a pagar o preço de uma chamada por colaborador — `consultaServiceHttp.listarConsulta` (`src/adapters/http/consultaService.http.ts`) detecta quando `de`/`ate` caem no mesmo mês e estende `data_fim` até o dia 1º do mês seguinte (`primeiroDiaDoProximoMes`, novo em `src/utils/periodo.ts`) — sai da condição "mesmo mês" da API sem excluir nenhum dia do período pedido pelo usuário. O mês seguinte que pode vazar por causa disso (com dado real ou não) é descartado do resultado antes de devolver, comparando `mes de referencia` contra o `filtro.de`/`filtro.ate` originais — nunca chega a aparecer pro usuário.
- **Motivo:** pedido do usuário — Consulta é histórico, não deve inventar entrada nenhuma que não existe de verdade.
- **Impacto:** nenhuma mudança de contrato com a API. Testes novos em `consultaService.http.test.ts`: confirma que De=Até estende `data_fim` corretamente (inclusive virada de ano, testado em `periodo.test.ts` pra `primeiroDiaDoProximoMes`); confirma que um mês que vaza na resposta (mesmo com dado real fabricado no teste) é descartado do resultado final. Suíte completa: 330 testes, 330 passaram. `npx tsc -b --noEmit` e `npx oxlint` sem erros novos. `npm run build` sem erros. **Verificado ao vivo contra o backend real** (login master): filtro padrão (De=Até=julho/2026) na filial 101 (só colaborador 239 habilitado, sem lançamento real) passou a mostrar corretamente "Nenhuma premiação encontrada" em vez do cartão fantasma; em "Todas as filiais" também. Nenhum erro de console.

### 2026-08-20 — Exportação CSV pelo backend estendida para Consolidado PEV, Comissão, Descontos e Plano de Saúde (Lançamento)

- **O que foi modificado:** pedido do usuário — replicar nas 4 telas restantes o mesmo padrão de exportação já usado em Premiação/Consulta (entradas anteriores do mesmo dia): buscar o CSV pronto do backend em vez de montar no cliente. Antes de implementar, os 4 endpoints `/exportar-csv` (`GET /api/consolidado`, `/api/comissoes`, `/api/descontos-bonificacoes`, `/api/lancamentos`) foram testados ao vivo contra o backend real (login master) pra conferir as colunas de verdade, em vez de assumir que seriam iguais às da exportação client-side anterior — e **não eram**, em 2 dos 4 casos: Comissão e Descontos hoje exportavam **.xlsx (Excel, via SheetJS)**, não CSV; ao trocar pro CSV do backend, Comissão perde a coluna **PEV** (só existe ao vivo na tela, lida da Premiação — o backend não devolve) e ganha CPF/Função; Descontos perde **CPF e Mês Referência** (o backend não devolve nenhum dos dois nessa exportação). Consolidado PEV já era CSV→CSV (sem troca de formato), e ficou mais rico (ganha o detalhamento mês a mês do ciclo, Base de Cálculo e Adiantamento, que a versão anterior não expunha no CSV). Plano de Saúde (Lançamento) também era Excel→CSV, mas sem perda de coluna real (o backend sempre devolve as 8 colunas de Saúde, com Adicional/Coparticipação zeradas no Odontológico). Essas diferenças foram apresentadas ao usuário antes de implementar (`AskUserQuestion`), com a opção de fazer só as telas sem perda de coluna — escolhida **fazer as 4 telas mesmo assim**, aceitando as perdas de Comissão e Descontos.
  - **`utils/exportar.ts`**: `decodificarCsvBase64` extraído como helper compartilhado (antes só existia inline em `respostaPremiacoesAgrupadas.ts`) — reutilizado pelos 4 novos adapters HTTP. `baixarExcel` e toda a infraestrutura de carregamento do SheetJS via CDN foram **removidos por completo** — depois dessa mudança, nenhuma tela do projeto usa mais exportação Excel client-side (as 3 que usavam migraram pra CSV do backend nesta mesma entrada); `exportar.test.ts` (só testava `baixarExcel`) removido junto.
  - **`consolidadoPevService.http.ts`**: novo `exportarCSV(filial, anoCiclo)` — `GET /api/consolidado/exportar-csv?ano=...&filial=...` (mesmos parâmetros do `listarConsolidadoPev`, `filial` omitido em "Todas as filiais"). Mock replica as mesmas colunas do backend real, calculadas a partir do que já existe em `listarConsolidadoPev`.
  - **`comissaoService.http.ts`**: novo `exportarCSV(filial, mesReferencia)` — `GET /api/comissoes/exportar-csv?mes_de_referencia=...&filial=...`.
  - **`descontosService.http.ts`**: novo `exportarCSV(filial, mesReferencia)` — `GET /api/descontos-bonificacoes/exportar-csv?mes_de_referencia=...&filial=...` (parâmetro `mes_de_referencia` aplicado por convenção, igual ao endpoint de listagem — não foi possível confirmar ao vivo se o backend realmente filtra por ele, já que não existe nenhum lançamento de desconto/bonificação real no ambiente de teste no momento; documentado aqui pra revisitar se algum dia aparecer dado real pra testar).
  - **`planoSaudeService.http.ts`**: novo `exportarCSV(filial, mesReferencia, tipoPlano)` — `GET /api/lancamentos/exportar-csv?mes_de_referencia=...&tipo_plano=...&filial=...`.
  - As funções síncronas antigas (`exportarComissoesExcel`, `exportarDescontosExcel`, `exportarPlanoSaudeExcel`) foram removidas dos respectivos `services/*.ts` — `exportarCSV` agora faz parte da interface de cada serviço, igual já era o padrão em Premiação/Consulta. `Comissao.tsx`/`Descontos.tsx`/`LancamentoPlanoSaude.tsx`/`ConsolidadoPev.tsx` ganharam estado `exportando` e `carregando` no botão (antes Consolidado/Comissão/Descontos/Plano de Saúde eram síncronos ou só esperavam o SheetJS carregar). Estado `comissoesSalvas` em `Comissao.tsx` removido por ter ficado sem nenhum uso depois da troca (só existia pra alimentar a exportação Excel antiga).
  - `ROADMAP.md`: F3.PEV-09, F4.COM-09, F4.DESC-09 e F5.PS-LAN-09 atualizados pra descrever o novo comportamento e as colunas reais (iam apontar pra funções/formato que não existem mais).
- **Motivo:** pedido do usuário — unificar o padrão de exportação (backend gera o arquivo) nas telas que ainda montavam Excel/CSV no cliente.
- **Impacto:** mudança de formato de arquivo (Excel → CSV) em Comissão, Descontos e Plano de Saúde; mudança de colunas em Comissão (perde PEV) e Descontos (perde CPF e Mês Referência) — ambas confirmadas com o usuário antes de implementar. Consolidado PEV ganha colunas, sem perder nenhuma. Testes novos em `comissaoService.http.test.ts`, `consolidadoPevService.http.test.ts`, `descontosService.http.test.ts` e `planoSaudeService.http.test.ts` (busca o CSV pronto e dispara o download; erro em vez de baixar arquivo vazio quando não há dado); botões de "Exportar Excel da filial" renomeados pra "Exportar CSV da filial" em `Comissao.test.tsx`/`Descontos.test.tsx` (únicos testes de UI que checavam esse texto). Suíte completa: 335 testes, 335 passaram. `npx tsc -b --noEmit` e `npx oxlint` sem erros novos. `npm run build` sem erros. **Verificado ao vivo contra o backend real** (login master) só nos 4 endpoints `/exportar-csv` via chamada direta (columns/params, incluindo confirmação de que `filial` de fato filtra o resultado em Comissão/Lançamentos — testado com uma filial inexistente, que devolveu CSV vazio) — não verificado ao vivo através da UI nesta rodada (as 4 telas em si, clicando no botão de exportar no navegador), diferente das rodadas anteriores do dia.
